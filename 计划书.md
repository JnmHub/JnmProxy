# JnmProxy 后端计划书

## 1. 项目目标

使用 Go 语言实现一个基于机场订阅链接的本地代理池 IP 系统，先完成后端能力，前端等后端稳定后再启动。

核心目标：

- 支持维护多个机场订阅链接。
- 拉取订阅时指定 `User-Agent: clash/1.18.0`。
- 解析订阅节点、订阅用量、到期时间。
- 所有代理节点统一存储在一张节点表中，通过订阅来源、协议、分组关系等字段区分。
- 节点默认不自动分组，支持手动分组、批量分组、关键词自动分组。
- 支持 HTTP 代理入口和 SOCKS5/SOCKS5H 代理入口。
- 客户端访问本系统代理时必须携带凭证，否则拒绝。
- 凭证可绑定全部节点、指定分组或指定单个节点。
- 转发时根据凭证绑定范围选择节点，支持随机选择和固定节点。
- 使用 SQLite 本地数据库，启动时自动初始化。
- 使用内存缓存降低数据库压力。
- 流量统计先写内存，再延迟批量写入数据库。
- 不引入 Clash 内核，不重复造轮子，优先使用公开 Go 依赖实现协议和基础能力。

## 2. 可行性分析

### 2.1 总体可行性

可行，但需要明确一个关键点：机场订阅中的节点协议通常不只是普通 HTTP/SOCKS5，上游节点可能包含 `ss`、`ssr`、`vmess`、`vless`、`trojan`、`hysteria2`、`tuic` 等协议。如果不引入 Clash 内核，也不自己重写所有协议栈，后端需要选择成熟公开依赖作为“出站协议适配层”。

建议路线：

- 后端管理、缓存、调度、认证、分组、统计全部自研。
- Clash 订阅解析自研或使用轻量 YAML/URI 解析依赖。
- 出站代理协议优先接入成熟 Go 依赖，避免重复造轮子。
- 第一阶段先打通主链路，优先支持常见节点协议；不支持的节点保留入库但标记为 `unsupported`。

### 2.2 技术风险

| 风险 | 说明 | 处理方式 |
| --- | --- | --- |
| 上游协议复杂 | 机场节点协议很多，完整兼容成本高 | 分阶段支持，先支持主流协议，不支持节点入库但不参与调度 |
| 订阅格式不统一 | 有 Clash YAML、Base64 URI、Sing-box 等格式 | 解析器做多格式探测，解析失败记录错误 |
| 流量统计准确性 | 代理连接长、并发高，实时写库压力大 | 内存原子计数 + 定时批量 flush |
| 节点可用性 | 订阅节点可能失效或延迟过高 | 增加健康检查与失败熔断 |
| 凭证安全 | 代理凭证不能泄露 | 密码哈希存储，日志脱敏 |
| 数据一致性 | 订阅刷新会新增、更新、失效节点 | 使用稳定指纹匹配节点，软删除/禁用旧节点 |

### 2.3 明确不做

当前后端阶段不做：

- 不做前端页面。
- 不引入 Clash 内核。
- 不实现订阅规则、分流规则、策略组规则。
- 不做公网多节点分布式部署。
- 不做复杂计费系统。
- 不默认公开无认证代理。
- 不在日志中输出完整订阅链接、代理凭证、节点敏感参数。

## 3. 技术选型

### 3.1 基础栈

- 语言：Go 1.22+。
- 数据库：SQLite。
- 数据访问：`database/sql` + SQLite driver。
- 配置：YAML 或 TOML 本地配置文件 + 环境变量覆盖。
- HTTP API：`net/http` + `chi` 或同级轻量路由库。
- 定时任务：Go 原生 `time.Ticker` + 可恢复任务调度器。
- 日志：`slog`。
- YAML 解析：`gopkg.in/yaml.v3`。
- 密码哈希：`bcrypt` 或 `argon2id`。

### 3.2 代理协议依赖策略

本项目自己的职责是代理池管理系统，不重复实现大型代理协议栈。

候选路线：

1. 优先评估 `sing-box`/`sing` 相关 Go 模块作为出站协议适配层。
2. 如某些协议集成成本过高，则先支持 HTTP、SOCKS5、Shadowsocks、Trojan、VMess/VLESS 中成本最低的一批。
3. 每个节点入库时保存原始配置，协议适配层按能力决定是否可用。
4. 未支持协议不删除，只标记为 `unsupported`，方便后续迭代。

验收要求：

- 后端代理入口必须支持 HTTP 和 SOCKS5 用户名密码认证。
- 至少完成一种真实上游协议的端到端转发验证后，才能进入代理池调度验收。
- 后续协议扩展通过 `OutboundAdapter` 接口新增，不污染订阅、分组、凭证模块。

## 4. 系统架构

### 4.1 模块划分

```text
cmd/jnmproxy
  └── 启动入口、配置加载、依赖装配

internal/config
  └── 配置结构、默认值、环境变量覆盖

internal/db
  └── SQLite 连接、迁移、事务工具

internal/model
  └── 数据模型与领域枚举

internal/repository
  └── 数据库读写封装

internal/cache
  └── 节点、分组、凭证、订阅、统计内存缓存

internal/subscription
  └── 订阅拉取、Header 解析、节点解析、刷新任务

internal/grouping
  └── 手动分组、批量分组、关键词自动分组

internal/auth
  └── 凭证创建、密码校验、绑定范围解析

internal/proxy
  └── HTTP/SOCKS5 入站代理、认证、流量计数

internal/outbound
  └── 出站节点协议适配、连接拨号、健康检查

internal/stats
  └── 流量聚合、延迟写库、统计查询

internal/scheduler
  └── 订阅刷新、健康检查、统计 flush 定时任务

internal/api
  └── 后端 REST API
```

### 4.2 请求主链路

```text
客户端
  -> HTTP/SOCKS5 入站代理
  -> 读取并校验凭证
  -> 从内存缓存读取凭证绑定策略
  -> 根据策略选择节点
       - 全部节点随机
       - 指定分组内随机
       - 指定单个节点固定
  -> 出站协议适配器建立连接
  -> 双向转发流量
  -> 内存统计连接、上行、下行、失败原因
  -> 定时批量写入 SQLite
```

### 4.3 订阅刷新主链路

```text
定时任务/手动触发
  -> 使用订阅 URL + User-Agent 拉取内容
  -> 解析 subscription-userinfo 响应头
  -> 探测订阅正文格式
  -> 解析节点列表
  -> 生成节点稳定指纹
  -> 新增/更新节点
  -> 标记本次未出现的旧节点为失效或禁用
  -> 刷新内存缓存
  -> 记录刷新日志与错误
```

## 5. 数据库设计

### 5.1 表清单

- `subscriptions`：订阅链接。
- `subscription_refresh_logs`：订阅刷新日志。
- `proxy_nodes`：统一代理节点表。
- `proxy_groups`：分组表。
- `proxy_node_groups`：节点与分组多对多关系。
- `group_keywords`：关键词分组规则。
- `credentials`：访问本代理系统的凭证。
- `credential_bindings`：凭证绑定范围。
- `traffic_stats_hourly`：小时级流量统计。
- `traffic_stats_daily`：天级流量统计。
- `node_health_checks`：节点健康检查记录。
- `system_settings`：系统配置项。

### 5.2 `subscriptions`

字段：

- `id`：主键。
- `name`：订阅名称。
- `url`：订阅链接，日志中必须脱敏。
- `user_agent`：默认 `clash/1.18.0`。
- `refresh_interval_seconds`：自动刷新间隔。
- `enabled`：是否启用。
- `last_refresh_at`：最近刷新时间。
- `next_refresh_at`：下次刷新时间。
- `last_status`：`success`、`failed`、`never`。
- `last_error`：最近错误，敏感信息脱敏。
- `upload_bytes`：订阅已上传流量。
- `download_bytes`：订阅已下载流量。
- `total_bytes`：订阅总流量。
- `expire_at`：订阅到期时间。
- `created_at` / `updated_at`。

说明：

- 订阅用量优先从 `subscription-userinfo` Header 解析。
- Header 常见格式：`upload=...; download=...; total=...; expire=...`。
- 如订阅服务商不返回 Header，则保留为空。

### 5.3 `proxy_nodes`

字段：

- `id`：主键。
- `subscription_id`：所属订阅。
- `subscription_node_key`：节点在订阅内的稳定指纹。
- `name`：节点名称。
- `protocol`：节点协议，如 `ss`、`vmess`、`trojan`、`socks5`。
- `server`：服务器地址。
- `port`：端口。
- `raw_uri`：原始 URI 形式，若存在。
- `raw_config_json`：原始节点配置 JSON。
- `adapter_status`：`supported`、`unsupported`、`error`。
- `enabled`：是否启用。
- `alive_status`：`unknown`、`alive`、`dead`。
- `last_seen_at`：最近一次订阅刷新出现时间。
- `last_checked_at`：最近健康检查时间。
- `latency_ms`：最近延迟。
- `fail_count`：连续失败次数。
- `created_at` / `updated_at`。

说明：

- 所有节点放在这一张表中。
- 通过 `subscription_id` 识别节点来源。
- 通过 `proxy_node_groups` 实现一个节点加入多个分组。
- 通过 `adapter_status` 控制是否参与代理池。

### 5.4 `proxy_groups`

字段：

- `id`：主键。
- `name`：分组名称，唯一。
- `description`：备注。
- `auto_created`：是否由关键词分组自动创建。
- `created_at` / `updated_at`。

### 5.5 `proxy_node_groups`

字段：

- `node_id`：节点 ID。
- `group_id`：分组 ID。
- `created_at`。

唯一约束：

- `node_id + group_id` 唯一。

### 5.6 `group_keywords`

字段：

- `id`：主键。
- `name`：规则名称。
- `keywords`：关键词文本，使用 `|` 分割。
- `case_sensitive`：是否大小写敏感。
- `enabled`：是否启用。
- `created_at` / `updated_at`。

规则：

- 输入示例：`香港|HK|Hong Kong`。
- 匹配节点名称。
- 命中的分组名称默认使用命中的关键词本身。
- 如分组不存在则自动创建。
- 支持选择部分规则执行，也支持全选一键分组。

### 5.7 `credentials`

字段：

- `id`：主键。
- `username`：用户名，唯一。
- `password_hash`：密码哈希。
- `enabled`：是否启用。
- `bind_mode`：`all`、`group`、`node`。
- `selection_policy`：`random`、`fixed`。
- `remark`：备注。
- `created_at` / `updated_at`。

说明：

- HTTP 代理使用 Basic Proxy-Authorization。
- SOCKS5 使用 username/password authentication。
- 用户示例中的 `15376259491:00hhg5210` 属于代理访问凭证。
- 凭证明文只在创建或重置时出现，数据库只保存哈希。

### 5.8 `credential_bindings`

字段：

- `id`：主键。
- `credential_id`：凭证 ID。
- `target_type`：`group` 或 `node`。
- `target_id`：分组 ID 或节点 ID。
- `created_at`。

规则：

- `bind_mode=all` 时不需要绑定记录。
- `bind_mode=group` 时可绑定一个或多个分组，调度时在这些分组内随机选择节点。
- `bind_mode=node` 时绑定一个节点，默认固定使用该节点。

### 5.9 统计表

`traffic_stats_hourly` 字段：

- `id`。
- `bucket_at`：小时桶时间。
- `credential_id`。
- `subscription_id`。
- `node_id`。
- `group_id`：若请求由分组策略选中，可记录命中的分组。
- `connections`。
- `success_connections`。
- `failed_connections`。
- `upload_bytes`。
- `download_bytes`。
- `created_at` / `updated_at`。

`traffic_stats_daily` 字段同上，用于天级聚合。

## 6. 缓存设计

### 6.1 缓存内容

启动后加载：

- 启用的订阅。
- 启用且可用的节点。
- 节点与分组关系。
- 启用的凭证与绑定关系。
- 关键词规则。

运行时维护：

- 凭证认证缓存。
- 调度候选节点缓存。
- 节点失败计数。
- 节点健康状态。
- 流量统计增量。

### 6.2 缓存刷新原则

- 管理 API 修改订阅、节点、分组、凭证后，立即刷新相关缓存。
- 订阅刷新完成后，刷新节点缓存和分组缓存。
- 关键词分组执行完成后，刷新分组缓存。
- 统计缓存不实时写库，由 stats 模块定时 flush。

### 6.3 并发原则

- 高频读取路径使用 `sync.RWMutex` 或原子结构。
- 代理转发路径不得每次查询数据库。
- 数据库写入失败时，统计增量保留在内存并下次重试。
- 进程异常退出可能丢失最近一个 flush 周期内的统计，这是可接受的 MVP 风险。

## 7. 后端 API 设计

所有 API 先给后端和未来前端使用，建议统一前缀 `/api/v1`。

### 7.1 订阅 API

- `POST /api/v1/subscriptions`：新增订阅。
- `GET /api/v1/subscriptions`：订阅列表。
- `GET /api/v1/subscriptions/{id}`：订阅详情。
- `PUT /api/v1/subscriptions/{id}`：更新订阅。
- `DELETE /api/v1/subscriptions/{id}`：删除或禁用订阅。
- `POST /api/v1/subscriptions/{id}/refresh`：手动刷新订阅。
- `GET /api/v1/subscriptions/{id}/refresh-logs`：刷新日志。

### 7.2 节点 API

- `GET /api/v1/nodes`：节点列表，支持按订阅、协议、分组、状态筛选。
- `GET /api/v1/nodes/{id}`：节点详情。
- `PUT /api/v1/nodes/{id}`：更新节点启用状态、备注等。
- `POST /api/v1/nodes/batch`：批量启用、禁用、加入分组、移出分组。
- `POST /api/v1/nodes/{id}/check`：单节点健康检查。
- `POST /api/v1/nodes/check`：批量健康检查。

### 7.3 分组 API

- `POST /api/v1/groups`：创建分组。
- `GET /api/v1/groups`：分组列表。
- `PUT /api/v1/groups/{id}`：更新分组。
- `DELETE /api/v1/groups/{id}`：删除分组。
- `POST /api/v1/groups/{id}/nodes`：批量添加节点到分组。
- `DELETE /api/v1/groups/{id}/nodes`：批量移出节点。

### 7.4 关键词分组 API

- `POST /api/v1/group-keywords`：创建关键词规则。
- `GET /api/v1/group-keywords`：规则列表。
- `PUT /api/v1/group-keywords/{id}`：更新规则。
- `DELETE /api/v1/group-keywords/{id}`：删除规则。
- `POST /api/v1/group-keywords/apply`：选择部分规则或全部规则执行自动分组。

### 7.5 凭证 API

- `POST /api/v1/credentials`：创建凭证。
- `GET /api/v1/credentials`：凭证列表。
- `GET /api/v1/credentials/{id}`：凭证详情。
- `PUT /api/v1/credentials/{id}`：更新凭证绑定、启用状态、备注。
- `POST /api/v1/credentials/{id}/reset-password`：重置密码。
- `DELETE /api/v1/credentials/{id}`：删除或禁用凭证。

### 7.6 统计 API

- `GET /api/v1/stats/overview`：总体流量概览。
- `GET /api/v1/stats/credentials`：按凭证统计。
- `GET /api/v1/stats/nodes`：按节点统计。
- `GET /api/v1/stats/subscriptions`：按订阅统计。
- `GET /api/v1/stats/timeseries`：按小时或天查询趋势。

### 7.7 系统 API

- `GET /api/v1/system/health`：系统健康。
- `GET /api/v1/system/settings`：系统配置。
- `PUT /api/v1/system/settings`：更新可运行时调整的配置。

## 8. 代理入口设计

### 8.1 HTTP 代理

监听地址示例：

- `127.0.0.1:1081`。

支持：

- 普通 HTTP 转发。
- HTTPS `CONNECT` 隧道。
- `Proxy-Authorization: Basic ...` 认证。

失败行为：

- 无凭证：返回 `407 Proxy Authentication Required`。
- 凭证错误：返回 `407`。
- 无可用节点：返回 `503 Service Unavailable`。

### 8.2 SOCKS5/SOCKS5H 代理

监听地址示例：

- `127.0.0.1:1080`。

支持：

- SOCKS5 username/password 认证。
- 客户端使用 `socks5h://username:password@host:port` 时，域名解析交给代理链路。

失败行为：

- 不支持无认证方法。
- 凭证错误直接拒绝。
- 无可用节点返回 SOCKS5 failure。

### 8.3 调度策略

凭证调度规则：

- 未指定分组或节点：从全部可用节点中随机。
- 指定分组：从该分组可用节点中随机。
- 指定单节点：固定使用该节点。

可用节点条件：

- `enabled=true`。
- `adapter_status=supported`。
- `alive_status != dead` 或死节点熔断时间已过。
- 所属订阅启用且未过期，过期策略可配置。

随机策略：

- MVP 使用均匀随机。
- 后续可增加权重、延迟优先、失败率优先。

## 9. 订阅解析设计

### 9.1 拉取要求

默认请求头：

```http
User-Agent: clash/1.18.0
```

可选增强：

- 超时配置，默认 20 秒。
- 重试配置，默认失败重试 1 次。
- 日志记录状态码、内容长度、解析结果数量。
- 日志不记录完整订阅 URL。

### 9.2 格式探测顺序

1. 尝试解析 Clash YAML。
2. 尝试解析 Base64 文本订阅。
3. 尝试按行解析 URI 节点。
4. 解析失败则记录 refresh log。

### 9.3 节点指纹

节点稳定指纹建议由以下字段生成：

```text
subscription_id + protocol + server + port + name + 关键认证字段哈希
```

用途：

- 同一订阅刷新时识别旧节点。
- 防止重复插入。
- 节点名称变化时尽量保留历史统计。

## 10. 关键词自动分组设计

### 10.1 规则输入

用户可以创建规则：

```text
香港|HK|Hong Kong
日本|JP|Japan
美国|US|United States
```

### 10.2 执行方式

支持：

- 单条规则执行。
- 多选规则执行。
- 全选规则一键执行。

执行逻辑：

1. 拆分 `|` 得到关键词列表。
2. 遍历节点名称。
3. 命中关键词后，以命中的关键词作为分组名称。
4. 分组不存在则创建。
5. 将节点加入该分组。
6. 已存在关系跳过。

### 10.3 注意事项

- 默认大小写不敏感。
- 一个节点可命中多个关键词，因此可加入多个分组。
- 自动创建的分组 `auto_created=true`。
- 手动分组不会被关键词执行清空。

## 11. 流量统计设计

### 11.1 统计维度

至少统计：

- 凭证维度。
- 节点维度。
- 订阅维度。
- 分组维度。
- 总体维度。
- 小时维度。
- 天维度。

### 11.2 内存聚合 Key

建议 key：

```text
bucket_time + credential_id + subscription_id + node_id + selected_group_id
```

### 11.3 Flush 策略

- 默认每 10 秒或 30 秒批量写库。
- 写库使用 upsert 累加。
- 写库成功后清除对应内存增量。
- 写库失败保留增量并记录错误。

### 11.4 统计准确性

- 连接开始时记录连接数。
- 转发过程中包装 reader/writer 统计上下行字节。
- 连接结束时记录成功或失败。
- 上游拨号失败也要记录失败次数。

## 12. 安全设计

- 管理 API 后续必须加管理端鉴权；MVP 可先只监听 `127.0.0.1`。
- 代理入口必须强制凭证认证。
- 凭证密码只存哈希。
- 订阅 URL、节点密码、token、UUID、用户密码写日志前必须脱敏。
- 默认监听本地地址，不默认暴露公网。
- SQLite 文件权限限制为当前用户可读写。
- API 返回节点详情时默认隐藏敏感字段，必要时提供显式开关。

## 13. 配置设计

配置文件建议：`config.yaml`。

示例：

```yaml
server:
  api_addr: "127.0.0.1:8080"

proxy:
  http_addr: "127.0.0.1:1081"
  socks_addr: "127.0.0.1:1080"

database:
  path: "./data/jnmproxy.db"

subscription:
  default_user_agent: "clash/1.18.0"
  default_refresh_interval_seconds: 3600
  request_timeout_seconds: 20

stats:
  flush_interval_seconds: 10

security:
  redact_logs: true
```

## 14. 开发阶段计划

### 阶段 1：项目骨架与数据库

任务：

- 初始化 Go module。
- 创建目录结构。
- 增加配置加载。
- 增加 SQLite 连接。
- 增加数据库迁移。
- 建立核心表结构。
- 增加基础启动命令。

验收：

- `go test ./...` 可运行。
- 启动后自动创建 SQLite 数据库。
- 所有表可自动初始化。

### 阶段 2：订阅管理与解析

任务：

- 实现订阅 CRUD。
- 实现指定 `User-Agent` 拉取订阅。
- 解析 `subscription-userinfo`。
- 实现 Clash YAML 解析。
- 实现 Base64/URI 格式探测。
- 节点统一入库。
- 刷新日志入库。

验收：

- 可以添加多个订阅。
- 可以手动刷新订阅。
- 可以看到订阅用量、总量、到期时间。
- 可以看到每个订阅下的节点。
- 不需要订阅规则，只保存节点。

### 阶段 3：分组与关键词分组

任务：

- 实现分组 CRUD。
- 实现节点手动加入/移出分组。
- 实现批量节点分组。
- 实现关键词规则 CRUD。
- 实现多选/全选关键词自动分组。
- 实现不存在分组自动创建。

验收：

- 节点默认不分组。
- 一个节点可以加入多个分组。
- 关键词通过 `|` 分割可以匹配节点名称。
- 命中关键词后按关键词名创建或使用分组。

### 阶段 4：凭证与调度缓存

任务：

- 实现凭证 CRUD。
- 实现密码哈希存储。
- 实现凭证绑定全部节点、分组、单节点。
- 实现节点候选集合缓存。
- 实现凭证认证缓存。
- 实现随机调度与固定节点调度。

验收：

- 无凭证不能使用代理。
- 凭证可绑定全部节点。
- 凭证可绑定指定分组。
- 凭证可绑定指定节点。
- 调度过程不依赖每次查库。

### 阶段 5：代理入口与出站适配

任务：

- 实现 HTTP 代理入口。
- 实现 SOCKS5/SOCKS5H 代理入口。
- 接入出站协议适配接口。
- 至少实现一批可用协议适配。
- 实现连接失败处理。
- 实现节点失败计数与临时熔断。

验收：

- 客户端可使用 `http://username:password@127.0.0.1:1081`。
- 客户端可使用 `socks5h://username:password@127.0.0.1:1080`。
- 凭证绑定全部节点时可随机选节点。
- 凭证绑定分组时只从分组内选节点。
- 凭证绑定节点时固定使用该节点。

### 阶段 6：流量统计

任务：

- 包装代理连接统计上下行字节。
- 实现内存聚合。
- 实现定时批量写入小时表。
- 实现天级聚合。
- 增加统计查询 API。

验收：

- 可以看到凭证流量。
- 可以看到节点流量。
- 可以看到订阅流量。
- 高频代理请求不会每次写数据库。

### 阶段 7：定时任务与健康检查

任务：

- 实现每个订阅独立刷新间隔。
- 实现启动后恢复 next refresh。
- 实现手动刷新不影响定时刷新稳定性。
- 实现节点健康检查任务。
- 实现健康状态缓存。

验收：

- 每个订阅可单独设置刷新时间。
- 定时任务能自动刷新订阅。
- 健康检查能标记节点可用或不可用。
- 不可用节点不参与正常调度，熔断后可重新检查。

### 阶段 8：管理 API 完整化与测试

任务：

- 补齐所有 REST API。
- 增加统一错误返回格式。
- 增加分页、筛选、排序。
- 增加单元测试。
- 增加关键链路集成测试。
- 增加 README 后端启动说明。

验收：

- 后端能力可通过 API 完整操作。
- 后端测试通过。
- README 能指导本地启动、添加订阅、创建凭证、使用代理。

### 阶段 9：前端准备，不实施前端

任务：

- 固化 API 响应结构。
- 输出未来前端页面清单。
- 整理接口文档。

验收：

- 后端不包含前端页面。
- 前端开发可直接基于 API 文档启动。

## 15. 里程碑顺序

必须按以下主线推进：

1. 项目骨架与数据库。
2. 订阅管理与节点入库。
3. 分组与关键词分组。
4. 凭证与缓存调度。
5. HTTP/SOCKS5 代理入口。
6. 出站协议适配。
7. 流量统计。
8. 定时刷新与健康检查。
9. API 完整化与测试。
10. 后端验收完成后，再讨论前端。

如果过程中遇到问题：

- 先修复阻塞主线的问题。
- 不做与当前阶段无关的过度优化。
- 修复后必须回到本计划主线。
- 若计划文件被手动更新，后续执行以最新 `计划书.md` 为准。

## 16. MVP 验收标准

MVP 完成时必须满足：

- SQLite 自动创建并完成迁移。
- 可以添加、刷新多个订阅。
- 订阅刷新使用 `User-Agent: clash/1.18.0`。
- 可以解析订阅用量、到期时间。
- 所有节点统一入库并带订阅来源标识。
- 节点默认不分组。
- 可以手动、批量、关键词自动分组。
- 一个节点可以加入多个分组。
- 可以创建多个代理访问凭证。
- 凭证可绑定全部节点、指定分组、指定节点。
- HTTP 和 SOCKS5/SOCKS5H 入口必须认证。
- 代理请求可按凭证策略选择节点。
- 流量统计走内存聚合并延迟写库。
- 后端 API 能覆盖核心管理能力。
- 不包含前端实现。

