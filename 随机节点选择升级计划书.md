# JnmProxy 随机节点选择升级计划书

> 目标：在不改变现有凭证绑定逻辑、不增加数据库压力的前提下，把当前“每次独立随机抽节点”升级为“尽量小次数内不重复”的低算力随机算法。

## 1. 背景说明

当前代理访问凭证支持三种绑定模式：

```text
全部节点：从全部可用节点里选择
指定分组：从绑定分组里的可用节点里选择
指定节点：固定使用一个节点
```

当前 `random` 策略的实际行为是：

```text
每次请求都从候选节点里随机抽一个
```

也就是说，假设候选节点有 10 个，每次请求都是重新抽奖一次。

这个算法简单、快，但是有一个明显问题：

```text
短时间内可能连续多次抽到同一个节点
```

从数学上说这是正常随机现象，但从代理池使用体验上看，会让人感觉“随机不均匀”。

## 2. 当前问题

### 2.1 小次数内容易重复

例如分组里有 10 个节点，连续请求 5 次，当前算法可能出现：

```text
节点 A
节点 A
节点 C
节点 A
节点 B
```

这不是 bug，而是普通随机本身的结果。

### 2.2 代理池利用率不够平滑

如果某个凭证短时间内请求量不大，普通随机可能导致：

- 某几个节点频繁被打中。
- 其他节点很久没有被用到。
- 健康节点数量很多，但实际使用分布不够均匀。

### 2.3 不能靠数据库解决

本项目已经明确要用内存缓存降低数据库压力，所以不能每次请求都写数据库记录“上次选了谁”。

随机选择状态应该放在内存里。

## 3. 升级目标

### 3.1 功能目标

升级后希望达到：

- 小次数请求内尽量不重复选中同一节点。
- 一轮内尽量把候选节点都使用一次。
- 一轮结束后重新随机洗牌，继续保持随机性。
- 候选节点变化时自动适应。
- 节点失败熔断后不会继续优先选中失败节点。
- 固定节点模式不受影响。

### 3.2 性能目标

必须满足：

- 每次选择节点尽量保持 `O(1)`。
- 只有候选节点变化或一轮用完时才做 `O(n)` 洗牌。
- 不增加每次请求的数据库查询。
- 不增加每次请求的数据库写入。
- 内存占用可控。

### 3.3 稳定性目标

必须满足：

- 不影响 SOCKS5 / HTTP 代理认证。
- 不影响现有流量统计。
- 不影响凭证绑定模式。
- 不影响健康检查和失败熔断。
- 不改变 API 入参结构。
- 不要求前端立刻改动。

## 4. 不做的事情

本次升级不做：

- 不引入复杂负载均衡系统。
- 不引入 Redis。
- 不新增数据库表。
- 不新增强制前端配置项。
- 不做按流量权重分配。
- 不做按延迟权重分配。
- 不做按地区权重分配。
- 不做用户级会话保持。

这些能力以后可以扩展，但本次主线只解决：

```text
随机节点短时间重复率过高
```

## 5. 可选算法对比

## 5.1 普通随机

当前使用的就是这个。

逻辑：

```text
每次从候选节点里随机一个
```

优点：

- 实现最简单。
- 算力最低。
- 长期看整体公平。

缺点：

- 小次数内可能重复很多。
- 用户体感不够均匀。

结论：

```text
不适合当前代理池体验要求
```

## 5.2 轮询 Round Robin

逻辑：

```text
节点 1 -> 节点 2 -> 节点 3 -> 节点 1
```

优点：

- 一轮内不会重复。
- 算力很低。

缺点：

- 太规律。
- 容易被看出顺序。
- 不够随机。

结论：

```text
适合固定负载均衡，但不太适合代理池随机选择
```

## 5.3 最近窗口排除

逻辑：

```text
记录最近 N 次选中的节点，下次随机时避开这些节点
```

优点：

- 可以减少重复。
- 思路直观。

缺点：

- 窗口大小不好设置。
- 候选节点少时容易没有可选节点。
- 实现比洗牌袋更绕。

结论：

```text
可以做，但不是当前最优
```

## 5.4 Shuffle Bag 随机洗牌袋

逻辑：

```text
先把候选节点随机洗牌成一个队列。
每次请求从队列里拿下一个。
一轮拿完后，再重新洗牌。
```

例如候选节点是：

```text
A B C D E
```

第一次洗牌后可能是：

```text
C A E B D
```

接下来 5 次请求就是：

```text
C -> A -> E -> B -> D
```

这一轮内不会重复。

用完后再洗牌，比如：

```text
B E A D C
```

继续下一轮。

优点：

- 一轮内天然不重复。
- 仍然有随机性。
- 每次选择很快。
- 实现简单。
- 很适合代理池。

缺点：

- 需要在内存里为凭证保存一个小队列。
- 候选节点变化时需要重建队列。

结论：

```text
本次升级采用 Shuffle Bag
```

## 6. 最终方案

本次采用：

```text
每个凭证一个随机洗牌袋
```

大白话：

```text
每个代理账号自己有一个“节点抽签袋”。
这个账号能用哪些节点，就把这些节点打乱后放进袋子。
每次请求拿一个。
拿完再重新洗牌。
```

这样不同凭证之间互不影响。

例如：

```text
凭证 A 绑定日本分组
凭证 B 绑定香港分组
凭证 C 使用全部节点
```

那么它们分别维护自己的洗牌袋：

```text
凭证 A：日本节点洗牌袋
凭证 B：香港节点洗牌袋
凭证 C：全部节点洗牌袋
```

## 7. 详细规则

### 7.1 全部节点模式

绑定模式：

```text
bind_mode = all
```

规则：

- 从全部可用节点生成候选列表。
- 使用洗牌袋选择。
- 一轮内尽量不重复。

### 7.2 指定分组模式

绑定模式：

```text
bind_mode = group
```

规则：

- 从绑定分组中收集节点。
- 多个分组里重复出现的节点要去重。
- 过滤失败熔断中的节点。
- 使用洗牌袋选择。

### 7.3 指定节点模式

绑定模式：

```text
bind_mode = node
```

规则：

- 只允许一个节点。
- 不走洗牌袋。
- 保持固定选择。

### 7.4 候选节点只有一个

规则：

- 直接返回这个节点。
- 不需要洗牌。

### 7.5 候选节点为空

规则：

- 返回当前已有错误：

```text
no candidate proxy nodes
```

不改变现有错误语义。

### 7.6 一轮结束

规则：

- 当前袋子用完后，重新洗牌。
- 如果候选节点数量大于 1，要尽量避免：

```text
上一轮最后一个节点 = 下一轮第一个节点
```

处理方式：

```text
如果新队列第一个等于上次选中的节点，就和队列中的其他随机位置交换
```

### 7.7 候选节点变化

候选节点可能因为这些原因变化：

- 订阅刷新。
- 手动启用/禁用节点。
- 节点健康检查状态变化。
- 代理转发失败触发熔断。
- 凭证绑定范围变化。
- 分组节点关系变化。

规则：

- 每次选择前仍然先算出当前候选节点。
- 如果候选列表和洗牌袋记录的不一致，就重建这个凭证的洗牌袋。
- 重建后从新袋子中选择。

## 8. 代码修改范围

### 8.1 主要文件

主要修改：

```text
internal/cache/store.go
```

原因：

- 当前节点选择逻辑就在内存缓存里。
- 所有代理请求最终都会走缓存选择节点。
- 这里改动能同时影响 HTTP 和 SOCKS5 入口。

### 8.2 测试文件

主要补充：

```text
internal/cache/store_test.go
```

需要增加 Shuffle Bag 相关单元测试。

## 9. 计划新增的数据结构

在 `Store` 中增加：

```go
selectionBags map[int64]*selectionBag
```

含义：

```text
key：credential_id
value：这个凭证自己的洗牌袋状态
```

新增内部结构：

```go
type selectionBag struct {
    candidates   []int64
    order        []int64
    index        int
    lastSelected int64
}
```

字段解释：

- `candidates`：当前候选节点快照。
- `order`：洗牌后的节点顺序。
- `index`：下一次要取的位置。
- `lastSelected`：上一次选中的节点，用来避免跨轮重复。

## 10. 核心选择流程

升级后的选择流程：

```text
1. 根据凭证找到候选节点。
2. 如果没有候选节点，返回错误。
3. 如果是固定策略，返回固定节点。
4. 如果候选节点只有一个，返回唯一节点。
5. 找到这个凭证对应的洗牌袋。
6. 如果洗牌袋不存在，创建。
7. 如果候选节点变化，重建洗牌袋。
8. 如果当前袋子用完，重新洗牌。
9. 从袋子中取下一个节点。
10. 返回节点。
```

## 11. 伪代码

```go
func (store *Store) selectRandomNodeLocked(credential CredentialSnapshot, candidateIDs []int64) int64 {
    if len(candidateIDs) == 1 {
        return candidateIDs[0]
    }

    bag := store.selectionBags[credential.ID]
    if bag == nil {
        bag = &selectionBag{}
        store.selectionBags[credential.ID] = bag
    }

    if !sameCandidates(bag.candidates, candidateIDs) {
        bag.reset(candidateIDs, store.random)
    }

    return bag.next(store.random)
}
```

洗牌袋逻辑：

```go
func (bag *selectionBag) next(random *rand.Rand) int64 {
    if bag.index >= len(bag.order) {
        bag.shuffle(random)
    }

    selected := bag.order[bag.index]
    bag.index++
    bag.lastSelected = selected
    return selected
}
```

避免跨轮重复：

```go
if len(order) > 1 && order[0] == lastSelected {
    swapIndex := 1 + random.Intn(len(order)-1)
    order[0], order[swapIndex] = order[swapIndex], order[0]
}
```

## 12. 并发安全

当前 `Store.Select` 已经使用：

```go
store.mu.Lock()
```

所以新增洗牌袋状态也在同一把锁里读写。

本次不需要额外加锁。

## 13. 缓存重载规则

`Store.Load` 重新加载数据库快照时：

- 节点列表可能变化。
- 凭证可能变化。
- 分组关系可能变化。

建议处理：

```text
保留 selectionBags map 也可以，因为候选列表变化时会自动重建。
```

更稳妥的处理：

```text
Load 完成后清空 selectionBags。
```

本次建议采用：

```text
Load 时清空 selectionBags
```

原因：

- 订阅刷新或分组变更后重新开始一轮更清晰。
- 实现更简单。
- 内存状态不会残留旧节点。

## 14. 失败熔断配合

当前已有失败熔断：

```text
连续失败达到阈值后，节点会临时从候选节点中过滤掉
```

升级后规则：

- 先过滤熔断节点。
- 再进入洗牌袋。
- 如果某个节点被熔断，候选列表变化，洗牌袋重建。
- 熔断到期后节点重新进入候选列表，洗牌袋再次重建。

这样可以避免：

```text
洗牌袋里还残留已经失败的节点
```

## 15. 配置策略

本次不新增前端配置。

现有：

```text
selection_policy = random
```

升级后语义变为：

```text
random = 洗牌袋随机
```

也就是说，用户不用重新配置。

如果以后需要兼容“完全普通随机”，可以再新增策略：

```text
pure_random
shuffle_bag
```

但本次不做，避免扩大范围。

## 16. API 影响

本次不改变 API。

这些接口不变：

```text
GET /api/v1/credentials
POST /api/v1/credentials
PUT /api/v1/credentials/{id}
```

这些字段不变：

```text
bind_mode
selection_policy
bindings
```

## 17. 前端影响

本次前端可以不改。

原因：

- 前端显示的仍然是 `random`。
- 用户理解上仍然是“随机”。
- 只是后端随机算法更平滑。

可选后续优化：

```text
在凭证页的选择策略说明里，把“随机”解释为“洗牌随机，尽量一轮不重复”
```

但这不是本次必须项。

## 18. 测试计划

### 18.1 单节点测试

场景：

```text
候选节点只有 1 个
```

预期：

- 每次都返回同一个节点。
- 不报错。

### 18.2 多节点一轮不重复

场景：

```text
候选节点有 5 个
连续选择 5 次
```

预期：

- 5 次结果都不重复。
- 5 个节点都出现一次。

### 18.3 第二轮继续随机

场景：

```text
候选节点有 5 个
连续选择 10 次
```

预期：

- 前 5 次是一轮完整排列。
- 后 5 次是另一轮完整排列。
- 每轮内部不重复。

### 18.4 跨轮边界避免重复

场景：

```text
第一轮最后一个节点是 A
第二轮第一个节点不应该还是 A
```

预期：

- 如果候选节点数量大于 1，尽量避免边界重复。

### 18.5 候选节点变化自动重建

场景：

```text
第一轮候选节点是 A B C
中途节点 C 不可用
候选节点变成 A B
```

预期：

- 洗牌袋自动重建。
- 后续不会再选到 C。

### 18.6 分组重复节点去重

场景：

```text
凭证绑定两个分组
同一个节点同时存在于两个分组
```

预期：

- 候选节点只出现一次。
- 洗牌袋里不会重复放入同一个节点。

### 18.7 固定节点不受影响

场景：

```text
bind_mode = node
selection_policy = fixed
```

预期：

- 永远返回绑定的节点。
- 不创建或不使用洗牌袋。

### 18.8 失败熔断过滤

场景：

```text
节点 A 连续失败达到阈值
```

预期：

- 节点 A 在熔断期内不会被选中。
- 熔断到期后可以重新进入候选列表。

## 19. 验证命令

后端测试：

```bash
GOCACHE=/tmp/jnm-go-cache go test ./internal/cache
```

全量测试：

```bash
GOCACHE=/tmp/jnm-go-cache go test ./...
```

如果涉及前端说明文案，再跑：

```bash
cd web && npm run build
```

本次如果只改后端缓存算法，前端构建不是强制项。

## 20. 分步骤执行计划

### 第一步：新增洗牌袋结构

修改：

```text
internal/cache/store.go
```

内容：

- 新增 `selectionBag` 结构。
- 新增 `selectionBags` 字段。
- 初始化 `selectionBags`。

验收：

- 代码能编译。
- 不影响现有测试。

### 第二步：实现洗牌袋选择函数

修改：

```text
internal/cache/store.go
```

内容：

- 新增候选列表比较函数。
- 新增洗牌函数。
- 新增 `selectRandomNodeLocked`。

验收：

- 多节点随机不再直接调用 `Intn` 选最终节点。
- 固定节点逻辑不受影响。

### 第三步：接入 `Store.Select`

修改：

```text
internal/cache/store.go
```

内容：

- 把当前普通随机替换为洗牌袋选择。
- 保留候选节点为空的错误逻辑。
- 保留固定策略逻辑。

验收：

- 所有现有代理入口继续使用同一套选择逻辑。

### 第四步：处理缓存重载

修改：

```text
internal/cache/store.go
```

内容：

- `Store.Load` 后清空 `selectionBags`。
- 防止旧节点状态残留。

验收：

- 订阅刷新、分组变更后不会使用旧洗牌袋。

### 第五步：补充单元测试

修改：

```text
internal/cache/store_test.go
```

内容：

- 一轮不重复测试。
- 第二轮测试。
- 候选变化测试。
- 固定节点不受影响测试。
- 熔断过滤测试。

验收：

```bash
GOCACHE=/tmp/jnm-go-cache go test ./internal/cache
```

必须通过。

### 第六步：全量回归

执行：

```bash
GOCACHE=/tmp/jnm-go-cache go test ./...
```

验收：

- 所有 Go 测试通过。
- 没有改变 API 行为。
- 没有引入数据库迁移。

### 第七步：阶段提交

如果 `.git` 权限正常，执行：

```bash
git add internal/cache/store.go internal/cache/store_test.go
git commit -m "feat: use shuffle bag for random node selection"
```

如果 `.git` 仍然只读，则只汇报原因，不强行处理。

## 21. 验收标准

本阶段完成后，必须满足：

- `random` 策略不再是每次独立普通随机。
- 同一个凭证在同一轮候选节点中尽量不重复。
- 候选节点变化后会自动重建选择队列。
- 固定节点模式行为不变。
- 不新增数据库表。
- 不增加每次请求数据库读写。
- `go test ./...` 通过。

## 22. 风险与处理

### 22.1 候选节点频繁变化

风险：

```text
如果节点频繁失败/恢复，洗牌袋可能频繁重建
```

处理：

- 当前候选列表本来每次都会计算。
- 重建只在列表变化时发生。
- 节点数量一般可控，成本可以接受。

### 22.2 凭证数量很多

风险：

```text
每个凭证一个洗牌袋会占用一些内存
```

处理：

- 每个袋子只保存节点 ID。
- 凭证数量和节点数量都在本地代理池可控范围内。
- 后续如有需要，可以增加空闲凭证袋清理。

### 22.3 用户误以为完全不重复

说明：

```text
候选节点一轮内不重复。
不同轮之间仍然可能再次选到同一节点。
候选节点变化后也可能重新开始一轮。
```

所以更准确的描述是：

```text
尽量避免小次数内重复，而不是永远不重复
```

## 23. 后续可扩展方向

本次完成后，后续可以继续扩展：

- 权重随机：按节点质量、延迟、失败率分配概率。
- 地区优先：优先使用指定国家/地区节点。
- 协议优先：优先使用稳定协议节点。
- 低延迟优先：结合健康检查延迟排序。
- 流量均衡：根据节点使用流量做平衡。
- 会话保持：同一个目标站点短时间内固定同一节点。

但这些都不是本次主线。

## 24. 最终结论

本次升级建议采用：

```text
Shuffle Bag 随机洗牌袋算法
```

它最符合当前需求：

- 算法简单。
- 算力低。
- 不查数据库。
- 不改 API。
- 一轮内尽量不重复。
- 用户体感比普通随机更均匀。

本计划执行时必须严格控制范围：

```text
只升级随机节点选择算法，不顺手改其它代理逻辑
```
