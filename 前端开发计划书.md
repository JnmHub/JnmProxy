# JnmProxy 前端开发计划书

> 目标：在不改偏后端主线的前提下，给 JnmProxy 做一个单页管理后台。先把后端已有能力完整管起来，前端不重新发明业务逻辑，只做“看得见、点得动、管得住”。

## 1. 总体结论

我建议前端就做单页应用，也就是 SPA。

原因很简单：

- 这是本地或内网管理后台，不是搜索引擎收录型网站。
- 主要功能都是表格、表单、状态、操作按钮。
- SPA 切页面快，体验适合管理系统。
- 后端已经提供 REST API，前端直接调用即可。
- 后续如果要打包成一个静态目录，也比较简单。

最终形态：

```text
浏览器
  -> JnmProxy 前端单页应用
  -> 调用 /api/v1 后端接口
  -> 管理订阅、节点、分组、凭证、统计、sing-box 状态
```

当前前端阶段不改代理核心逻辑。

## 2. 前端定位

这个前端不是普通官网，也不是营销页。

它是：

```text
代理池管理后台
```

主要用途：

- 看系统运行状态。
- 管理订阅链接。
- 管理代理节点。
- 管理分组。
- 管理关键词分组。
- 管理代理访问凭证。
- 查看流量统计。
- 查看 sing-box 协议支持状态。
- 手动刷新订阅。
- 手动健康检查节点。
- 手动重建节点 adapter。

一句话：

```text
前端就是 JnmProxy 后端能力的操作台。
```

## 3. 使用 UI/UX Skill 后的设计方向

本计划参考了项目根目录 `.codex/skills/ui-ux-pro-max/SKILL.md` 的设计建议。

结合当前项目类型，最终采用：

```text
专业深色数据管理后台
```

而不是：

```text
炫酷营销页
横向滚动展示页
过度动画页面
```

原因：

- 管理后台最重要的是效率。
- 节点、订阅、凭证都是高密度数据。
- 用户需要快速筛选、批量操作、看状态。
- 深色主题适合长期查看代理和节点状态。

## 4. 视觉风格

### 4.1 整体风格

建议风格：

- 深色后台。
- 专业技术感。
- 数据面板风。
- 高对比。
- 少动画。
- 强状态提示。
- 表格清晰。

关键词：

```text
Dark Admin Dashboard
Data Operations
Proxy Pool Control Panel
Technical SaaS
```

### 4.2 主色

推荐颜色：

- 背景深色：`#020617`
- 面板深色：`#0F172A`
- 边框颜色：`#1E293B`
- 主色蓝：`#3B82F6`
- 强调橙：`#F59E0B`
- 成功绿：`#22C55E`
- 危险红：`#EF4444`
- 警告黄：`#FACC15`
- 普通文字：`#E5E7EB`
- 次要文字：`#94A3B8`

大白话解释：

- 蓝色表示主要操作。
- 绿色表示正常、成功、在线。
- 红色表示失败、死亡、错误。
- 黄色表示警告、需要注意。
- 橙色用于重点按钮或关键提醒。

### 4.3 字体

推荐：

- 正文字体：`Fira Sans`
- 数字、协议、代码字段：`Fira Code`

原因：

- 这个项目有很多协议名、端口、流量、状态、JSON 字段。
- 等宽字体能让数字和配置看起来更整齐。

### 4.4 图标

统一使用 SVG 图标。

建议用：

- `lucide-react`

不建议：

- 使用 emoji 当图标。
- 每个页面图标风格不统一。

## 5. 推荐技术栈

建议使用：

```text
React + TypeScript + Vite + Tailwind CSS
```

配套库建议：

- 路由：`react-router-dom`
- 请求和缓存：`@tanstack/react-query`
- 表格：先自己封装轻量表格，复杂后再考虑 `@tanstack/react-table`
- 图表：`recharts`
- 图标：`lucide-react`
- 表单：先用受控组件，复杂后再考虑 `react-hook-form`
- 日期格式化：`dayjs`
- 通知提示：自定义 Toast 或轻量库

为什么这样选：

- React 生态成熟。
- Vite 启动快。
- TypeScript 能减少接口字段写错。
- Tailwind 写后台页面速度快。
- React Query 适合管理接口数据、刷新、加载状态。

## 6. 前端目录建议

建议在项目里新增：

```text
web/
  package.json
  vite.config.ts
  tsconfig.json
  index.html
  src/
    main.tsx
    app/
      App.tsx
      router.tsx
      providers.tsx
    api/
      client.ts
      types.ts
      subscriptions.ts
      nodes.ts
      groups.ts
      credentials.ts
      stats.ts
      system.ts
    components/
      layout/
        AppShell.tsx
        Sidebar.tsx
        Topbar.tsx
      ui/
        Button.tsx
        Card.tsx
        Badge.tsx
        Modal.tsx
        Drawer.tsx
        Table.tsx
        Input.tsx
        Select.tsx
        Toast.tsx
        EmptyState.tsx
        ConfirmDialog.tsx
      charts/
        MetricCard.tsx
        TrafficChart.tsx
    pages/
      DashboardPage.tsx
      SubscriptionsPage.tsx
      SubscriptionDetailPage.tsx
      NodesPage.tsx
      GroupsPage.tsx
      KeywordGroupsPage.tsx
      CredentialsPage.tsx
      StatsPage.tsx
      SystemPage.tsx
      SettingsPage.tsx
    hooks/
      useToast.ts
      useDebounce.ts
      useConfirm.ts
    utils/
      format.ts
      bytes.ts
      status.ts
    styles/
      globals.css
```

大白话解释：

- `api/` 专门放后端接口调用。
- `components/` 放通用组件。
- `pages/` 放每个页面。
- `utils/` 放格式化流量、时间、状态的函数。
- `app/` 放路由和整体应用壳子。

## 7. 页面整体布局

采用常见后台布局：

```text
左侧菜单 + 顶部状态栏 + 主内容区
```

布局结构：

```text
┌──────────────────────────────────────────────┐
│ 顶部栏：系统状态 / sing-box / 刷新按钮        │
├──────────────┬───────────────────────────────┤
│ 左侧菜单      │ 页面内容                       │
│ Dashboard    │ 表格 / 卡片 / 表单 / 图表       │
│ Subscriptions│                               │
│ Nodes        │                               │
│ Groups       │                               │
│ Credentials  │                               │
│ Stats        │                               │
│ System       │                               │
└──────────────┴───────────────────────────────┘
```

左侧菜单建议：

- 仪表盘
- 订阅管理
- 节点管理
- 分组管理
- 关键词分组
- 凭证管理
- 流量统计
- 系统状态
- 设置

顶部栏建议展示：

- API 在线状态。
- sing-box 是否启用。
- QUIC 是否启用。
- 当前时间。
- 手动刷新当前页面按钮。

## 8. 页面规划

## 8.1 仪表盘

路径建议：

```text
/
/dashboard
```

主要展示：

- 总连接数。
- 成功连接数。
- 失败连接数。
- 上传流量。
- 下载流量。
- 订阅数量。
- 节点数量。
- 可用节点数量。
- 死亡节点数量。
- sing-box 状态。
- QUIC 状态。

卡片示例：

```text
今日连接数
成功率
上传流量
下载流量
可用节点数
异常节点数
```

图表：

- 流量趋势。
- 成功/失败连接比例。
- 协议分布。
- 节点健康分布。

第一版可以先做总览卡片，图表后面补。

## 8.2 订阅管理页

路径建议：

```text
/subscriptions
```

功能：

- 订阅列表。
- 新增订阅。
- 编辑订阅。
- 删除订阅。
- 启用/禁用订阅。
- 手动刷新订阅。
- 查看订阅用量。
- 查看过期时间。
- 查看最近刷新状态。
- 查看下一次刷新时间。

列表字段建议：

- 名称。
- URL 简略显示。
- User-Agent。
- 刷新间隔。
- 是否启用。
- 最近状态。
- 已用流量。
- 总流量。
- 到期时间。
- 最近刷新时间。
- 下一次刷新时间。
- 操作。

操作按钮：

- 刷新。
- 查看节点。
- 查看日志。
- 编辑。
- 删除。

注意：

- 订阅 URL 比较敏感，默认中间打码。
- 删除订阅前必须二次确认。

## 8.3 订阅详情页

路径建议：

```text
/subscriptions/:id
```

功能：

- 展示订阅基本信息。
- 展示订阅下节点。
- 展示刷新日志。
- 展示流量用量。

刷新日志字段：

- 状态。
- HTTP 状态码。
- 节点数量。
- sing-box 支持数量。
- sing-box 错误数量。
- unsupported 数量。
- 错误原因。
- 开始时间。
- 结束时间。

大白话：

这个页面主要用来看“这条订阅最近有没有刷新成功，里面节点解析得怎么样”。

## 8.4 节点管理页

路径建议：

```text
/nodes
```

这是前端最核心的页面之一。

功能：

- 节点列表。
- 按订阅筛选。
- 按协议筛选。
- 按分组筛选。
- 按健康状态筛选。
- 按启用状态筛选。
- 搜索节点名称。
- 启用/禁用节点。
- 单节点健康检查。
- 批量健康检查。
- 批量启用。
- 批量禁用。
- 批量加入分组。
- 批量移出分组。
- 重建 sing-box adapter。

列表字段建议：

- 复选框。
- 节点名称。
- 协议。
- 服务器。
- 端口。
- 来源订阅。
- 分组。
- 启用状态。
- 健康状态。
- 延迟。
- 失败次数。
- sing-box 状态。
- 传输类型。
- 最后出现时间。
- 最后检查时间。
- 操作。

状态颜色建议：

- `alive`：绿色。
- `dead`：红色。
- `unknown`：灰色。
- `sing_box_status=supported`：绿色。
- `sing_box_status=error`：红色。
- `sing_box_status=unsupported`：灰色。

节点详情抽屉：

- 点击节点名称打开右侧抽屉。
- 展示节点完整信息。
- 展示 raw config。
- 展示 sing-box outbound JSON。
- 展示错误信息。

安全注意：

- `raw_config_json` 和 `sing_box_outbound_json` 里可能有密码、UUID。
- 前端默认折叠。
- 后续最好做敏感字段打码展示。

## 8.5 分组管理页

路径建议：

```text
/groups
```

功能：

- 分组列表。
- 新建分组。
- 编辑分组。
- 删除分组。
- 查看分组内节点。
- 给分组添加节点。
- 从分组移除节点。

列表字段：

- 分组名称。
- 描述。
- 是否自动创建。
- 节点数量。
- 创建时间。
- 更新时间。
- 操作。

交互建议：

- 点击分组打开详情。
- 详情里展示当前组内节点。
- 支持批量移出。

## 8.6 关键词分组页

路径建议：

```text
/keyword-groups
```

功能：

- 关键词规则列表。
- 新建关键词规则。
- 编辑关键词规则。
- 删除关键词规则。
- 启用/禁用规则。
- 多选规则执行。
- 全部规则一键执行。

规则字段：

- 规则名称。
- 关键词。
- 是否区分大小写。
- 是否启用。

关键词输入说明：

```text
香港|HK|日本|JP|新加坡|SG
```

大白话：

用户把关键词用 `|` 分开，系统用节点名称去匹配，匹配到了就自动创建或加入对应分组。

执行结果展示：

- 扫描节点数。
- 创建分组数。
- 命中节点数。
- 新增绑定关系数。

## 8.7 凭证管理页

路径建议：

```text
/credentials
```

功能：

- 凭证列表。
- 创建凭证。
- 编辑凭证。
- 删除凭证。
- 启用/禁用凭证。
- 重置密码。
- 设置绑定范围。
- 设置选择策略。

创建凭证表单：

- 用户名。
- 密码。
- 是否启用。
- 绑定模式：全部、分组、节点。
- 分组多选。
- 节点多选。
- 选择策略：随机、固定。
- 备注。

列表字段：

- 用户名。
- 是否启用。
- 绑定模式。
- 选择策略。
- 备注。
- 创建时间。
- 更新时间。
- 操作。

安全注意：

- 后端不返回密码哈希。
- 前端不要尝试展示旧密码。
- 重置密码只负责输入新密码。

## 8.8 流量统计页

路径建议：

```text
/stats
```

第一版功能：

- 展示总体连接数。
- 展示成功连接数。
- 展示失败连接数。
- 展示上传流量。
- 展示下载流量。

后续增强：

- 按小时趋势。
- 按天趋势。
- 按节点统计。
- 按凭证统计。
- 按订阅统计。
- 按分组统计。

图表建议：

- 折线图：流量趋势。
- 柱状图：节点流量排行。
- 饼图或环图：成功/失败比例。
- 面积图：上传/下载趋势。

## 8.9 系统状态页

路径建议：

```text
/system
```

功能：

- API 健康状态。
- sing-box 是否启用。
- sing-box 版本。
- 配置版本。
- 当前模式。
- 是否优先原生 HTTP/SOCKS。
- 是否启用 QUIC。
- 支持协议列表。
- GPL 许可证提示。

接口：

```text
GET /api/v1/system/health
GET /api/v1/system/sing-box
```

这个页面主要用于排查：

- 后端是不是活着。
- sing-box 是否开启。
- Hysteria2/TUIC 为什么不支持。
- 当前构建是否带 `with_quic`。

## 8.10 设置页

路径建议：

```text
/settings
```

第一版可以只做只读说明。

展示：

- 当前 API 地址。
- 当前代理入口地址。
- 当前项目版本。
- 配置文件说明。

后续再考虑：

- 在线修改部分配置。
- 保存配置。
- 重启提示。

当前不建议第一版就做在线改配置，避免复杂度过高。

## 9. API 对接计划

前端统一请求前缀：

```text
/api/v1
```

请求封装：

```text
src/api/client.ts
```

统一处理：

- JSON 请求。
- JSON 响应。
- 错误响应里的 `error` 字段。
- 请求超时。
- 加载状态。

错误响应格式：

```json
{"error":"错误说明"}
```

前端展示方式：

- 表单提交失败：Toast + 表单旁提示。
- 列表加载失败：页面中间 Empty/Error 状态。
- 删除失败：Toast。
- 批量操作失败：Toast + 保留选择。

## 10. 前端数据类型计划

建议在：

```text
web/src/api/types.ts
```

定义后端类型。

主要类型：

- `Subscription`
- `SubscriptionRefreshLog`
- `ProxyNode`
- `ProxyGroup`
- `GroupKeyword`
- `Credential`
- `TrafficOverview`
- `SystemHealth`
- `SingBoxStatus`

好处：

- 页面写错字段会被 TypeScript 提醒。
- 后端字段变了，前端容易定位。

## 11. 状态和标签设计

前端需要统一状态标签。

### 11.1 订阅状态

- `success`：绿色，成功。
- `failed`：红色，失败。
- `never`：灰色，从未刷新。

### 11.2 节点健康状态

- `alive`：绿色，可用。
- `dead`：红色，死亡。
- `unknown`：灰色，未知。

### 11.3 adapter 状态

- `supported`：绿色，支持。
- `unsupported`：灰色，不支持。
- `error`：红色，错误。

### 11.4 sing-box 状态

- `supported`：绿色。
- `unsupported`：灰色。
- `error`：红色。

## 12. 表格交互规范

管理后台最常用的是表格。

统一规范：

- 顶部放筛选区。
- 中间放表格。
- 表格左侧支持多选。
- 表格右侧放操作按钮。
- 批量操作按钮只有选中节点时显示。
- 危险操作必须二次确认。
- 长文本字段默认截断。
- 鼠标悬停显示完整内容或复制按钮。

节点表格必须重点优化。

第一版可以先做普通表格。

后续如果节点很多，再做：

- 分页。
- 虚拟列表。
- 列显示控制。
- 批量复制。

## 13. 表单交互规范

表单统一使用受控组件。

也就是：

```text
输入框的值由 React state 控制
```

创建或编辑时：

- 必填项明显标记。
- 保存按钮展示 loading。
- 成功后关闭弹窗或抽屉。
- 失败后不清空用户输入。
- 删除操作必须确认。

## 14. 弹窗和抽屉规范

建议：

- 创建、编辑用 Modal。
- 节点详情用 Drawer。
- 删除确认用 ConfirmDialog。
- 批量操作用 Modal。

原因：

- 节点详情内容多，抽屉比弹窗更适合。
- 简单表单用弹窗效率高。

## 15. 响应式计划

第一目标：

- 桌面端。
- 笔记本。
- 1440px 和 1024px 宽度体验好。

第二目标：

- 平板可用。
- 手机能基本查看和操作。

手机端策略：

- 左侧菜单收起。
- 表格变横向滚动。
- 详情用全屏抽屉。

## 16. 登录和权限计划

当前后端管理 API 还没有管理端鉴权。

所以前端第一版建议：

- 只用于本地或内网。
- 默认不暴露公网。
- 不做复杂多角色权限。

但是计划中必须预留：

- 登录页。
- Token 保存。
- 请求自动带 Token。
- 401 自动跳登录。

后端后续加管理端鉴权后，前端再接入。

## 17. 前端开发阶段拆分

## 阶段 1：前端脚手架

任务：

- 新建 `web/` 目录。
- 初始化 Vite + React + TypeScript。
- 接入 Tailwind CSS。
- 接入 React Router。
- 接入 React Query。
- 建立基础目录结构。
- 建立 API client。
- 建立基础布局。

验收：

- `npm run dev` 可启动。
- 能打开空白后台页面。
- 能请求 `/api/v1/system/health`。
- 页面有左侧菜单和顶部栏。

## 阶段 2：基础 UI 组件

任务：

- Button。
- Card。
- Badge。
- Table。
- Input。
- Select。
- Modal。
- Drawer。
- Toast。
- ConfirmDialog。
- EmptyState。
- LoadingState。

验收：

- 页面风格统一。
- 常见组件可复用。
- 点击、hover、focus 状态清晰。

## 阶段 3：仪表盘和系统状态

任务：

- 仪表盘总览卡片。
- 流量统计总览。
- API 健康状态。
- sing-box 状态。
- QUIC 状态。
- 支持协议列表。

验收：

- 能看到系统是否正常。
- 能看到 sing-box 是否启用。
- 能看到 QUIC 是否启用。
- 能看到总连接数和流量。

## 阶段 4：订阅管理

任务：

- 订阅列表。
- 创建订阅。
- 编辑订阅。
- 删除订阅。
- 手动刷新订阅。
- 订阅详情。
- 刷新日志。

验收：

- 可以完整管理订阅。
- 可以刷新订阅并看到结果。
- 可以看到订阅用量和过期时间。

## 阶段 5：节点管理

任务：

- 节点列表。
- 筛选。
- 节点详情抽屉。
- 启用/禁用。
- 单节点检查。
- 批量检查。
- 批量启用/禁用。
- 批量加入/移出分组。
- 重建 adapter。

验收：

- 可以清楚看到节点状态。
- 可以按协议、订阅、分组、健康状态筛选。
- 可以批量管理节点。
- 可以排查 sing-box 错误。

## 阶段 6：分组和关键词分组

任务：

- 分组列表。
- 创建/编辑/删除分组。
- 查看分组节点。
- 添加/移除节点。
- 关键词规则列表。
- 创建/编辑/删除关键词规则。
- 执行关键词分组。

验收：

- 可以手动分组。
- 可以关键词自动分组。
- 可以看到执行结果。

## 阶段 7：凭证管理

任务：

- 凭证列表。
- 创建凭证。
- 编辑凭证。
- 删除凭证。
- 重置密码。
- 绑定全部节点。
- 绑定分组。
- 绑定节点。
- 选择随机或固定策略。

验收：

- 可以创建代理使用账号密码。
- 可以控制账号能用哪些节点。
- 可以重置密码。

## 阶段 8：流量统计

任务：

- 总览统计。
- 上传/下载展示。
- 成功/失败展示。
- 基础图表。

验收：

- 能看到系统整体使用情况。
- 能看到流量和连接数。

## 阶段 9：联调和体验优化

任务：

- 全页面接口联调。
- 错误提示统一。
- loading 状态统一。
- 空状态统一。
- 删除确认。
- 批量操作确认。
- 响应式检查。
- 打包构建。

验收：

- `npm run build` 通过。
- 所有核心页面可用。
- 常见失败情况有提示。
- 不出现明显白屏。

## 18. 后端接口和页面映射

| 页面 | 后端接口 |
|---|---|
| 仪表盘 | `GET /stats/overview`、`GET /system/health`、`GET /system/sing-box` |
| 订阅列表 | `GET /subscriptions` |
| 创建订阅 | `POST /subscriptions` |
| 编辑订阅 | `PUT /subscriptions/{id}` |
| 删除订阅 | `DELETE /subscriptions/{id}` |
| 刷新订阅 | `POST /subscriptions/{id}/refresh` |
| 订阅日志 | `GET /subscriptions/{id}/refresh-logs` |
| 节点列表 | `GET /nodes` |
| 节点详情 | `GET /nodes/{id}` |
| 节点检查 | `POST /nodes/{id}/check` |
| 批量检查 | `POST /nodes/check` |
| 批量操作 | `POST /nodes/batch` |
| 重建 adapter | `POST /nodes/{id}/rebuild-adapter` |
| 分组列表 | `GET /groups` |
| 创建分组 | `POST /groups` |
| 编辑分组 | `PUT /groups/{id}` |
| 删除分组 | `DELETE /groups/{id}` |
| 分组加节点 | `POST /groups/{id}/nodes` |
| 分组移节点 | `DELETE /groups/{id}/nodes` |
| 关键词规则 | `GET /group-keywords` |
| 执行关键词 | `POST /group-keywords/apply` |
| 凭证列表 | `GET /credentials` |
| 创建凭证 | `POST /credentials` |
| 编辑凭证 | `PUT /credentials/{id}` |
| 重置密码 | `POST /credentials/{id}/reset-password` |
| 删除凭证 | `DELETE /credentials/{id}` |

## 19. 需要后端后续补充的接口

当前后端已经够第一版前端使用。

但为了体验更好，后续建议后端补：

- 节点列表分页。
- 节点名称搜索。
- 节点协议聚合统计。
- 分组节点数量字段。
- 订阅节点数量字段。
- 按小时流量趋势接口。
- 按节点流量排行接口。
- 管理后台登录鉴权。

第一版前端可以先不等这些接口，能做的先做。

## 20. 前端安全注意事项

必须注意：

- 不要展示凭证密码。
- 不要展示密码哈希。
- 订阅 URL 默认打码。
- 节点 raw config 默认折叠。
- sing-box outbound JSON 默认折叠。
- 删除操作必须确认。
- 批量危险操作必须确认。
- 管理后台不要直接暴露公网。

## 21. 构建和运行计划

前端开发命令建议：

```bash
cd web
npm install
npm run dev
```

生产构建：

```bash
cd web
npm run build
```

后续可以考虑：

- 前端单独跑一个 Vite dev server。
- 后端只提供 API。
- 生产环境把前端打包成静态文件。
- 后端以后可以选择内嵌静态文件服务。

第一版建议：

```text
前端和后端分开跑
```

等稳定后再考虑后端内嵌前端静态资源。

## 22. 开发纪律

前端阶段需要遵守：

- 不改后端代理核心逻辑。
- 不引入重型 UI 框架，避免项目复杂。
- 每完成一个可验收页面就测试。
- 每完成一个阶段可以 Git commit。
- 先做能用，再做漂亮。
- 表格和表单优先，动画靠后。
- 所有 API 错误必须有提示。
- 不把敏感字段直接大面积展开。

## 23. 第一版最小可用目标

第一版前端最少要做到：

- 能看仪表盘。
- 能创建和刷新订阅。
- 能查看节点。
- 能筛选节点。
- 能启用/禁用节点。
- 能给节点分组。
- 能创建凭证。
- 能绑定凭证到全部、分组或节点。
- 能查看系统和 sing-box 状态。
- 能查看基础流量统计。

只要这些完成，就已经是一个能用的管理后台。

## 24. 最终验收标准

前端计划完成时，需要满足：

- `web/` 前端项目可启动。
- 页面布局统一。
- 深色后台风格统一。
- 主要页面都能调用真实后端 API。
- 订阅管理可用。
- 节点管理可用。
- 分组管理可用。
- 关键词分组可用。
- 凭证管理可用。
- 系统状态可用。
- 流量总览可用。
- API 错误提示清楚。
- loading 和空状态完整。
- 打包构建成功。

## 25. 总结

前端就做 SPA 是合适的。

当前后端已经把核心能力准备好了，前端的任务就是：

```text
把这些能力整理成一个清晰好用的管理后台。
```

建议按阶段推进：

1. 先搭脚手架和布局。
2. 再做通用组件。
3. 再做仪表盘和系统状态。
4. 再做订阅和节点。
5. 再做分组和凭证。
6. 最后补统计图表和体验优化。

这样不会一开始就陷入复杂 UI，能快速看到成果。
