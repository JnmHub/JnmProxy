# JnmProxy 后端成果书

> 这份文档用大白话说明：现在这个后端已经能干什么、怎么工作、支持哪些协议、哪些地方已经做了保护、哪些能力还没有做。

## 1. 一句话总结

JnmProxy 现在已经是一个可运行的 Go 后端代理池系统。

它可以：

- 从机场订阅链接里拉取节点。
- 把节点统一保存到本地 SQLite 数据库。
- 给节点分组。
- 给使用代理的人创建账号密码。
- 根据账号绑定范围随机或固定选择节点。
- 对外提供 HTTP 代理和 SOCKS5 代理入口。
- 使用原生代码或内嵌 sing-box 去连接上游代理节点。
- 记录代理流量统计。
- 定时刷新订阅。
- 定时检查节点是否可用。
- 通过 REST API 管理订阅、节点、分组、凭证、统计和 sing-box 状态。

当前阶段只做后端，不包含前端页面。

## 2. 当前后端整体架构

现在系统大概是这样跑的：

```text
你的客户端
  -> JnmProxy HTTP/SOCKS5 入口
  -> 校验 JnmProxy 凭证
  -> 根据凭证绑定范围选择节点
  -> 走原生出站或 sing-box 出站
  -> 上游机场节点
  -> 目标网站
```

也就是说：

- 客户端只需要连接 JnmProxy。
- 客户端必须带 JnmProxy 里创建的账号密码。
- 节点怎么选、能不能用、分到哪个组，都是 JnmProxy 管。
- sing-box 只负责“复杂协议怎么连出去”，不负责管理逻辑。

## 3. 本地数据库能力

现在使用的是 SQLite 本地数据库。

默认数据库位置：

```text
./data/jnmproxy.db
```

已经完成的数据库能力：

- 程序启动时自动创建数据库目录。
- 程序启动时自动执行数据库迁移。
- 支持老版本数据库继续升级，不需要手动删库。
- 订阅、节点、分组、凭证、流量、健康检查等数据都落库。
- 节点新增、更新、消失都有对应状态维护。

主要表能力：

- `subscriptions`：保存订阅链接。
- `subscription_refresh_logs`：保存订阅刷新记录。
- `proxy_nodes`：保存所有代理节点。
- `proxy_groups`：保存节点分组。
- `proxy_node_groups`：保存节点和分组的多对多关系。
- `group_keywords`：保存关键词自动分组规则。
- `credentials`：保存代理使用凭证。
- `credential_bindings`：保存凭证绑定范围。
- `traffic_stats_hourly`：保存小时级流量统计。
- `traffic_stats_daily`：保存天级流量统计。
- `node_health_checks`：保存节点健康检查记录。
- `sing_box_engine_states`：为 sing-box 引擎状态预留。

## 4. 订阅管理能力

现在支持多个订阅链接。

创建订阅时可以配置：

- 订阅名称。
- 订阅 URL。
- 自定义 User-Agent。
- 自动刷新间隔。
- 是否启用。

如果不指定 User-Agent，默认使用：

```text
clash/1.18.0
```

这符合你最开始提到的机场订阅要求。

订阅刷新时会做这些事：

- 请求订阅链接。
- 读取订阅流量信息，例如已用流量、总流量、过期时间。
- 解析订阅里的节点。
- 保存节点到数据库。
- 更新已经存在的节点。
- 标记已经消失的节点为不可用。
- 生成 sing-box 出站配置。
- 记录刷新日志。
- 更新下一次刷新时间。

刷新失败时：

- 不会直接把程序搞崩。
- 会记录失败原因。
- 会写入刷新日志。

## 5. 订阅格式解析能力

现在支持这几类订阅内容：

- Clash YAML 订阅。
- Base64 编码的 URI 订阅。
- 一行一个节点 URI 的订阅。

当前会读取 Clash YAML 里的：

```yaml
proxies:
  - name: 节点名称
    type: 协议
    server: 服务器
    port: 端口
```

不会导入：

- Clash rules。
- Clash proxy-groups。
- rule-providers。
- DNS 规则。
- 分流规则。

原因是当前 JnmProxy 自己负责调度和分组，不让机场规则接管系统。

## 6. 节点入库能力

所有节点现在都放在同一张节点表里。

每个节点会保存：

- 节点 ID。
- 来源订阅 ID。
- 节点名称。
- 节点协议。
- 服务器地址。
- 端口。
- 原始 URI。
- 原始配置 JSON。
- 当前是否启用。
- 当前是否健康。
- 延迟。
- 失败次数。
- 最后出现时间。
- 最后检查时间。

同时还会保存 sing-box 相关字段：

- `sing_box_outbound_json`：转换后的 sing-box 出站配置。
- `sing_box_status`：sing-box 是否支持这个节点。
- `sing_box_error`：转换或初始化失败原因。
- `sing_box_version`：生成配置时使用的 sing-box 版本。
- `udp_supported`：是否具备 UDP 能力标记。
- `transport_type`：传输层类型，例如 `ws`、`grpc`、`tcp`。

这样后面做前端时，可以直接展示节点状态。

## 7. 节点协议支持情况

### 7.1 默认构建已经支持的协议

默认构建下，当前 TCP 转发已经支持：

- `http`
- `https`
- `socks`
- `socks5`
- `socks5h`
- `ss`
- `shadowsocks`
- `vmess`
- `vless`
- `trojan`

其中：

- HTTP、HTTPS、SOCKS5 简单协议优先走 JnmProxy 原生出站。
- SS、VMess、VLESS、Trojan 等复杂协议走内嵌 sing-box 出站。

### 7.2 需要 QUIC 构建标签的协议

下面这些协议依赖 QUIC：

- `hysteria2`
- `hy2`
- `tuic`

如果要支持它们，需要用这个方式构建或运行：

```bash
go run -tags with_quic ./cmd/jnmproxy
```

专项测试命令：

```bash
go test -tags with_quic ./internal/singbox -run TestQUICProtocolTCPTransfers -count=1 -v
```

如果没有开启 `with_quic`：

- Hysteria2/TUIC 节点仍然会保存到数据库。
- 但 `sing_box_status` 会是 `error`。
- `sing_box_error` 会提示需要 `-tags with_quic`。
- 这些节点不会进入代理调度。

### 7.3 暂未完整支持的协议

当前不承诺完整支持：

- `ssr`
- 私有魔改协议。
- 机场自定义非标准协议。
- 必须依赖系统 TUN 或路由权限的场景。

## 8. sing-box 集成成果

现在已经引入：

```text
github.com/sagernet/sing-box v1.13.8
```

重要点：

- 不需要用户系统安装 sing-box 命令。
- 不启动外部 sing-box 服务。
- sing-box 是作为 Go 依赖编进 JnmProxy 程序里的。
- JnmProxy 仍然负责入口认证、节点选择、分组、统计和 API。
- sing-box 只负责具体协议连接上游节点。

当前采用的模式是：

```text
JnmProxy 自有入站
  -> JnmProxy 调度选节点
  -> sing-box outbound adapter
  -> 上游代理节点
```

也就是说，sing-box 没有接管整个系统，只是被 JnmProxy 当作协议能力使用。

## 9. 代理入口能力

现在对外提供两个代理入口。

默认 HTTP 代理：

```text
127.0.0.1:1081
```

默认 SOCKS5 代理：

```text
127.0.0.1:1080
```

使用 HTTP 代理示例：

```bash
curl -x http://用户名:密码@127.0.0.1:1081 https://example.com
```

使用 SOCKS5H 代理示例：

```bash
curl --proxy socks5h://用户名:密码@127.0.0.1:1080 https://example.com
```

注意：

- 不带账号密码不会通过。
- 账号密码不是机场节点的账号密码。
- 账号密码是 JnmProxy 自己创建的代理访问凭证。

## 10. 凭证管理能力

现在可以创建多个代理使用凭证。

每个凭证包含：

- 用户名。
- 密码。
- 是否启用。
- 绑定模式。
- 选择策略。
- 备注。

密码不会明文保存，会保存哈希。

凭证绑定模式有三种：

### 10.1 绑定全部节点

模式：

```text
all
```

意思是：

- 这个凭证可以使用所有可用节点。
- 请求代理时从全部可用节点里选。

### 10.2 绑定分组

模式：

```text
group
```

意思是：

- 这个凭证只能使用指定分组里的节点。
- 如果绑定多个分组，就从这些分组的节点里选。

### 10.3 绑定单个节点

模式：

```text
node
```

意思是：

- 这个凭证只能使用指定节点。
- 适合固定出口 IP 的场景。

选择策略有两种：

- `random`：随机选节点。
- `fixed`：固定选择候选列表里的第一个节点。

## 11. 分组管理能力

现在节点支持多个分组。

一个节点可以：

- 不属于任何分组。
- 属于一个分组。
- 同时属于多个分组。

已经支持：

- 创建分组。
- 查看分组。
- 更新分组。
- 删除分组。
- 批量把节点加入分组。
- 批量把节点移出分组。
- 凭证绑定分组。

这满足“一个节点可以加入多个分组”的需求。

## 12. 关键词自动分组能力

现在支持关键词规则。

规则里可以写：

```text
香港|HK|日本|JP
```

系统会根据节点名称匹配关键词。

例如节点名称是：

```text
香港 HK 01
```

如果规则里有 `香港` 或 `HK`，就会自动加入对应分组。

当前支持：

- 创建关键词规则。
- 更新关键词规则。
- 删除关键词规则。
- 启用或禁用规则。
- 区分大小写或不区分大小写。
- 一键执行全部规则。
- 只执行指定规则。
- 没有分组时自动创建分组。

## 13. 节点调度能力

客户端请求代理时，系统会这样选节点：

1. 根据代理用户名找到凭证。
2. 判断凭证是否启用。
3. 根据凭证绑定模式找候选节点。
4. 过滤已经死亡或熔断中的节点。
5. 根据选择策略选一个节点。
6. 用这个节点连接目标网站。

如果节点连接失败：

- 会记录失败。
- 失败次数达到阈值后会临时熔断。
- 熔断期间这个节点不会被选中。

如果后续健康检查恢复：

- 节点会重新进入调度候选。

## 14. 内存缓存能力

系统现在不会每次代理请求都查数据库。

启动时会加载：

- 凭证。
- 凭证绑定。
- 节点。
- 节点分组。

这些内容会放到内存缓存里。

好处：

- 代理请求速度更快。
- 数据库压力更小。
- 高频代理请求不会一直打 SQLite。

在这些操作之后，会重新加载缓存：

- 订阅刷新。
- 修改节点启用状态。
- 修改分组绑定。
- 修改凭证。
- 健康检查结果变化。

## 15. 健康检查能力

系统现在支持节点健康检查。

健康检查会：

- 找出可检查节点。
- 通过节点连接健康检查目标。
- 成功则标记为 `alive`。
- 失败则标记为 `dead`。
- 记录延迟。
- 记录失败原因。
- 写入健康检查日志表。
- 刷新运行时缓存。

现在健康检查已经覆盖：

- 原生 HTTP/HTTPS/SOCKS5 节点。
- sing-box 支持的复杂协议节点。

也就是说，SS、VMess、VLESS、Trojan 等节点可以通过 sing-box adapter 做健康检查。

## 16. 流量统计能力

现在有流量统计功能。

统计维度包括：

- 凭证。
- 订阅。
- 节点。
- 分组。
- 小时。
- 天。

统计内容包括：

- 连接数。
- 成功连接数。
- 失败连接数。
- 上传字节数。
- 下载字节数。

实现方式是：

```text
代理请求产生统计
  -> 先写内存
  -> 定时批量写入 SQLite
```

这样不会每转发一点流量就写数据库，能降低数据库压力。

默认刷新间隔：

```text
10 秒
```

## 17. REST API 能力

当前后端 API 前缀：

```text
/api/v1
```

已经提供这些模块：

### 17.1 系统 API

- `GET /system/health`：查看后端是否活着。
- `GET /system/sing-box`：查看 sing-box 状态、版本、协议支持、是否启用 QUIC。

### 17.2 订阅 API

- `POST /subscriptions`：创建订阅。
- `GET /subscriptions`：查看订阅列表。
- `GET /subscriptions/{id}`：查看订阅详情。
- `PUT /subscriptions/{id}`：更新订阅。
- `DELETE /subscriptions/{id}`：删除订阅。
- `POST /subscriptions/{id}/refresh`：手动刷新订阅。
- `GET /subscriptions/{id}/refresh-logs`：查看刷新日志。
- `GET /subscriptions/{id}/nodes`：查看订阅下节点。

### 17.3 节点 API

- `GET /nodes`：查看节点列表。
- `GET /nodes/{id}`：查看节点详情。
- `PUT /nodes/{id}`：启用或禁用节点。
- `POST /nodes/{id}/check`：检查单个节点。
- `POST /nodes/check`：批量检查节点。
- `POST /nodes/batch`：批量启用、禁用、加组、移出分组。
- `POST /nodes/{id}/rebuild-adapter`：重建节点 sing-box 适配器。

### 17.4 分组 API

- `POST /groups`：创建分组。
- `GET /groups`：查看分组列表。
- `GET /groups/{id}`：查看分组详情。
- `PUT /groups/{id}`：更新分组。
- `DELETE /groups/{id}`：删除分组。
- `POST /groups/{id}/nodes`：批量加节点到分组。
- `DELETE /groups/{id}/nodes`：批量从分组移除节点。

### 17.5 关键词分组 API

- `POST /group-keywords`：创建关键词规则。
- `GET /group-keywords`：查看关键词规则。
- `PUT /group-keywords/{id}`：更新关键词规则。
- `DELETE /group-keywords/{id}`：删除关键词规则。
- `POST /group-keywords/apply`：执行关键词自动分组。

### 17.6 凭证 API

- `POST /credentials`：创建凭证。
- `GET /credentials`：查看凭证列表。
- `GET /credentials/{id}`：查看凭证详情。
- `PUT /credentials/{id}`：更新凭证。
- `POST /credentials/{id}/reset-password`：重置密码。
- `DELETE /credentials/{id}`：删除凭证。

### 17.7 统计 API

- `GET /stats/overview`：查看总体流量统计。

## 18. 定时任务能力

现在系统有后台定时任务。

主要有两个：

### 18.1 订阅刷新任务

会定期检查哪些订阅到时间了。

到时间后：

- 自动刷新订阅。
- 更新节点。
- 更新缓存。

### 18.2 健康检查任务

会定期检查节点是否可用。

检查后：

- 更新节点健康状态。
- 记录延迟。
- 更新缓存。

默认配置在 `config.example.yaml` 里。

## 19. 配置文件能力

现在支持 `config.yaml` 配置。

可以配置：

- API 监听地址。
- HTTP 代理监听地址。
- SOCKS5 代理监听地址。
- SQLite 数据库路径。
- 默认订阅 User-Agent。
- 订阅请求超时时间。
- 统计写库间隔。
- 定时刷新间隔。
- 健康检查间隔。
- sing-box 是否启用。
- sing-box engine 缓存数量。
- sing-box engine 空闲回收时间。
- sing-box 连接超时时间。
- 日志脱敏开关。

也支持部分环境变量覆盖配置，例如数据库路径、sing-box 模式、UDP 开关等。

## 20. 测试和验证成果

现在项目已经有较完整的后端测试。

默认全量测试命令：

```bash
go test ./...
```

已经通过。

QUIC 专项测试命令：

```bash
go test -tags with_quic ./internal/singbox -run TestQUICProtocolTCPTransfers -count=1 -v
```

也已经通过。

当前测试覆盖了：

- 配置加载。
- 数据库迁移。
- 订阅解析。
- 订阅刷新。
- 节点入库。
- sing-box outbound 配置转换。
- sing-box adapter 拨号。
- SS/VMess/VLESS/Trojan 本地闭环转发。
- Hysteria2/TUIC 在 `with_quic` 下的本地闭环转发。
- HTTP 代理入口。
- SOCKS5 代理入口。
- 并发代理请求。
- 订阅刷新期间缓存重载不打断已有连接。
- adapter 空闲回收和重建。
- 健康检查。
- 流量统计。
- API 基础流程。

## 21. 已提交的主要阶段成果

当前 sing-box 阶段已经按小阶段提交过。

主要提交包括：

- `feat: verify embedded sing-box outbound`
- `feat: add sing-box config and schema`
- `feat: add sing-box outbound converter`
- `feat: add sing-box outbound adapter`
- `test: verify sing-box core protocol transfers`
- `feat: integrate sing-box health checks`
- `feat: link subscription refresh to sing-box`
- `feat: expose sing-box management APIs`
- `test: add sing-box stability coverage`
- `test: verify quic protocol transfer support`

这些提交说明后端不是一次性乱改，而是按阶段完成和验证的。

## 22. 当前还没有做的事情

下面这些还没做，或者不是当前阶段目标。

### 22.1 前端还没做

现在只有后端。

还没有：

- 管理页面。
- 登录页面。
- 节点列表页面。
- 分组管理页面。
- 凭证管理页面。
- 统计图表页面。

### 22.2 管理 API 还没有鉴权

当前 API 默认建议只监听：

```text
127.0.0.1
```

后面做前端时，需要给管理 API 加登录鉴权。

注意：

- 代理入口已经有凭证认证。
- 这里说的是管理后台 API 鉴权。

### 22.3 UDP 入站代理还没做

当前主要支持 TCP。

还没做：

- SOCKS5 UDP ASSOCIATE。
- UDP 统计。
- UDP 健康检查。

### 22.4 TUN 和透明代理没做

当前不做：

- TUN。
- 透明代理。
- 系统路由表修改。
- 网卡级代理。

### 22.5 订阅规则没有导入

当前不导入：

- Clash rules。
- proxy-groups。
- rule-providers。
- DNS 分流规则。

这是有意为之。

因为 JnmProxy 现在自己管理节点池和调度。

## 23. 当前使用方式示例

### 23.1 启动后端

```bash
go run ./cmd/jnmproxy
```

如果要支持 Hysteria2/TUIC：

```bash
go run -tags with_quic ./cmd/jnmproxy
```

### 23.2 新增订阅

```bash
curl -X POST http://127.0.0.1:8080/api/v1/subscriptions \
  -H 'Content-Type: application/json' \
  -d '{"name":"示例订阅","url":"https://example.com/sub","refresh_interval_seconds":3600}'
```

### 23.3 手动刷新订阅

```bash
curl -X POST http://127.0.0.1:8080/api/v1/subscriptions/1/refresh
```

### 23.4 创建代理凭证

```bash
curl -X POST http://127.0.0.1:8080/api/v1/credentials \
  -H 'Content-Type: application/json' \
  -d '{"username":"15376259491","password":"00hhg5210","bind_mode":"all","selection_policy":"random"}'
```

### 23.5 使用 HTTP 代理

```bash
curl -x http://15376259491:00hhg5210@127.0.0.1:1081 https://example.com
```

### 23.6 使用 SOCKS5H 代理

```bash
curl --proxy socks5h://15376259491:00hhg5210@127.0.0.1:1080 https://example.com
```

## 24. 后面适合继续做什么

建议下一阶段优先做：

1. 管理 API 鉴权。
2. 前端管理页面。
3. 节点列表和节点详情展示。
4. 订阅刷新日志展示。
5. 分组和关键词分组页面。
6. 凭证管理页面。
7. 流量统计图表。
8. 节点健康检查按钮和结果展示。
9. 更细的 sing-box 错误提示。
10. UDP 支持单独设计。

## 25. 最终结论

当前后端已经完成了代理池系统的核心骨架和主要后端能力。

它现在不是单纯的“订阅解析器”，而是已经具备：

- 节点管理。
- 凭证认证。
- 分组调度。
- 多协议转发。
- sing-box 内嵌协议能力。
- 健康检查。
- 流量统计。
- 后端管理 API。
- 定时任务。
- SQLite 本地持久化。

后续主要工作就是在这个后端基础上继续做前端管理台，以及补充管理端鉴权和更多运维体验。
