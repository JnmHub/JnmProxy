# JnmProxy 下一步开发计划书

> 目标：在现有后端、前端和随机节点选择算法已经成型的基础上，下一阶段把系统做成“更好排错、更好管理、更好部署”的可用版本。

## 1. 当前项目状态

现在 JnmProxy 已经具备这些基础能力：

- 可以拉取机场订阅。
- 可以解析并保存节点。
- 可以管理节点分组。
- 可以创建代理访问凭证。
- 可以按全部节点、指定分组、指定节点选择节点。
- 已经支持 HTTP 和 SOCKS5 代理入口。
- 已经接入 sing-box 作为 Go 依赖，不需要系统安装 sing-box。
- 已经支持复杂协议节点的出站配置生成。
- 已经有 SQLite 本地数据库。
- 已经有内存缓存，避免每次请求查数据库。
- 已经有流量统计。
- 已经有前端 SPA 管理后台基础页面。
- 随机节点选择已经升级为 `Shuffle Bag` 洗牌袋算法，能尽量避免小次数内重复选中同一节点。

大白话说：

```text
系统主干已经能跑了，下一步不是继续堆大功能，而是把“看得懂、查得出、用得稳、部署简单”补齐。
```

## 2. 下一阶段总方向

我建议下一阶段主线定为：

```text
JnmProxy 管理与诊断增强阶段
```

这个阶段重点不是继续换代理核心，而是把下面三件事做好：

```text
1. 出问题时能快速知道为什么
2. 管理节点和凭证时更直观
3. 后端和前端能更方便地一起启动和部署
```

## 3. 为什么不先做其它方向

### 3.1 不建议马上做复杂权重算法

随机洗牌袋刚完成，已经解决“小次数重复”的主要问题。

下一步不建议立刻做：

- 延迟权重。
- 流量权重。
- 地区权重。
- 协议权重。

原因：

```text
权重调度需要更多稳定数据支撑，现在更应该先把诊断和管理体验补齐。
```

### 3.2 不建议马上做多数据库支持

当前 SQLite 符合本项目本地代理池定位。

下一步不建议立刻做：

- MySQL。
- PostgreSQL。
- Redis。

原因：

```text
当前瓶颈不是数据库，而是管理体验和排错体验。
```

### 3.3 不建议马上做大量新协议

sing-box 已经覆盖了主要复杂协议能力。

下一步不建议偏离到：

- 自己重写协议实现。
- 大规模替换 sing-box 集成方式。

原因：

```text
现在最需要的是让已有协议跑得可观察、可诊断、可管理。
```

## 4. 下一阶段核心目标

### 4.1 系统诊断目标

让用户打开管理后台就能看懂：

- 当前程序是否正常。
- 当前是否启用了 sing-box。
- 当前是否启用了 `with_quic`。
- 当前是否启用了 `with_utls`。
- 当前支持哪些协议。
- 当前健康检查目标是什么。
- 节点为什么失败。
- 节点为什么不进入随机池。

### 4.2 节点管理目标

让用户可以更方便地：

- 按订阅筛选节点。
- 按分组筛选节点。
- 按协议筛选节点。
- 按存活状态筛选节点。
- 按 sing-box 支持状态筛选节点。
- 批量启用/禁用节点。
- 批量健康检查节点。
- 查看节点失败原因。
- 查看节点原始配置和 sing-box 出站配置。

### 4.3 凭证管理目标

让用户更直观看到：

- 每个凭证绑定了哪些分组。
- 每个凭证固定了哪个节点。
- 每个凭证当前策略到底是随机还是固定。
- 每个凭证可以复制一条测试命令。

例如：

```bash
curl --proxy socks5h://用户名:密码@127.0.0.1:1080 https://httpbin.org/ip
```

### 4.4 部署目标

让项目最终更容易运行：

```text
一个 Go 程序同时提供 API、HTTP/SOCKS5 代理、前端静态页面
```

大白话：

```text
用户不需要单独启动前端开发服务器。
后端启动后，浏览器访问管理地址就能打开后台。
```

## 5. 本阶段不做的事情

为了避免偏离主线，本阶段不做：

- 不重新设计数据库大结构。
- 不替换 sing-box。
- 不重写 HTTP/SOCKS5 代理入口。
- 不做多用户管理后台权限系统。
- 不做公网部署方案。
- 不做移动端 App。
- 不做复杂商业化套餐系统。
- 不做自动购买机场订阅。
- 不做规则分流系统。

本阶段只围绕：

```text
诊断、管理、部署收口
```

## 6. 阶段一：系统诊断页增强

### 6.1 目标

把 `系统状态` 页面做成真正的排错入口。

用户遇到代理失败时，第一时间能看懂：

```text
是程序没开？
是构建标签不对？
是 sing-box 没启用？
是节点协议不支持？
是健康检查目标不通？
是节点本身失败？
```

### 6.2 后端检查

现有接口：

```text
GET /api/v1/system/health
GET /api/v1/system/sing-box
```

下一步需要确认这些信息是否完整：

- `enabled`
- `version`
- `mode`
- `supported_protocols`
- `quic_enabled`
- `utls_enabled`
- `health_check_target`
- `adapter_configured`
- `max_active_engines`
- `engine_idle_timeout_seconds`
- `engine_dial_timeout_seconds`

如果缺字段，只补字段，不重做接口。

### 6.3 前端增强

修改页面：

```text
web/src/pages/SystemPage.tsx
```

增加：

- 构建能力卡片。
- 协议支持列表。
- 运行建议提示。
- 常见错误解释。
- 推荐启动命令展示。

推荐展示命令：

```bash
go run -tags "with_quic with_utls" ./cmd/jnmproxy
```

### 6.4 验收标准

- 用户能看到 `QUIC` 是否启用。
- 用户能看到 `uTLS` 是否启用。
- 用户能看到支持协议列表。
- 用户能看到健康检查目标。
- `npm run build` 通过。
- `go test ./...` 通过。

## 7. 阶段二：节点页增强

### 7.1 目标

节点页要从“能看列表”升级为：

```text
节点排查和批量管理中心
```

### 7.2 前端筛选能力

修改页面：

```text
web/src/pages/NodesPage.tsx
```

增加筛选：

- 订阅筛选。
- 分组筛选。
- 协议筛选。
- 存活状态筛选。
- sing-box 状态筛选。
- 启用状态筛选。

后端已有节点列表接口支持部分筛选：

```text
GET /api/v1/nodes
```

已有 query 能力需要复用，不重复造接口。

### 7.3 节点详情抽屉

点击节点后打开详情抽屉。

展示：

- 节点名称。
- 节点协议。
- 服务器和端口。
- 所属订阅。
- 所属分组。
- 是否启用。
- 存活状态。
- 最近检查时间。
- 延迟。
- 失败次数。
- sing-box 状态。
- sing-box 错误。
- 原始配置 JSON。
- sing-box 出站 JSON。

### 7.4 批量操作

支持：

- 批量启用。
- 批量禁用。
- 批量健康检查。

如果后端已经有接口就复用。

如果缺少必要接口，只补最小接口，不扩展过度。

### 7.5 验收标准

- 节点页能按常用条件筛选。
- 能打开详情查看失败原因。
- 能批量启用/禁用节点。
- 能触发健康检查。
- `npm run build` 通过。
- 相关 Go 测试通过。

## 8. 阶段三：凭证页体验补强

### 8.1 目标

凭证页现在已经修复绑定模式和策略矛盾问题。

下一步是把它做得更直观：

```text
用户不用点编辑，也能知道这个凭证到底会从哪里选节点
```

### 8.2 表格展示增强

修改页面：

```text
web/src/pages/CredentialsPage.tsx
```

增强表格绑定列：

- `全部节点`：显示“全部可用节点随机”。
- `指定分组`：直接显示分组名称。
- `指定节点`：直接显示节点名称。

不要只显示：

```text
目标 2 个
```

应该显示：

```text
日本节点、香港节点
```

或者：

```text
固定：东京 01
```

### 8.3 测试命令复制

增加复制按钮。

支持生成：

```bash
curl --proxy socks5h://用户名:密码@127.0.0.1:1080 https://httpbin.org/ip
```

注意：

- 前端不能拿到旧密码明文。
- 创建时可以显示一次测试命令。
- 已存在凭证只能显示用户名占位和手动填写密码提示。

### 8.4 验收标准

- 凭证表格能直接看到绑定范围。
- 固定节点不会显示分组选择。
- 分组随机不会显示固定节点选择。
- 创建/编辑逻辑不回退。
- `npm run build` 通过。

## 9. 阶段四：订阅详情与刷新日志增强

### 9.1 目标

订阅页要让用户能看懂：

```text
订阅有没有成功拉取？
拉到了多少节点？
哪些节点支持？
哪些节点不支持？
失败原因是什么？
流量用量和过期时间是什么？
```

### 9.2 订阅列表增强

修改页面：

```text
web/src/pages/SubscriptionsPage.tsx
web/src/pages/SubscriptionDetailPage.tsx
```

增加：

- 流量使用进度条。
- 过期时间提示。
- 最近刷新状态。
- 最近错误原因。
- 节点数量统计。
- sing-box 支持/失败统计。

### 9.3 刷新日志展示

详情页展示：

- 刷新时间。
- HTTP 状态码。
- 节点数量。
- 支持数量。
- 不支持数量。
- 错误信息。

### 9.4 验收标准

- 用户能看懂订阅刷新结果。
- 刷新失败原因可见。
- 订阅流量和过期时间可见。
- `npm run build` 通过。

## 10. 阶段五：前后端一体化托管

### 10.1 目标

把前端构建产物交给 Go 后端托管。

最终效果：

```text
启动 JnmProxy
打开 http://127.0.0.1:8080
直接进入管理后台
```

### 10.2 技术方向

建议使用 Go 标准库：

```go
embed
net/http
```

流程：

```text
1. 前端执行 npm run build
2. 生成 web/dist
3. Go 程序 embed web/dist
4. 后端同时提供 /api/v1 和前端页面
5. SPA 路由 fallback 到 index.html
```

### 10.3 注意事项

不要影响：

- HTTP 代理监听端口。
- SOCKS5 代理监听端口。
- API 路由。

管理后台 HTTP 服务要和代理入口分清楚。

### 10.4 配置建议

配置里明确区分：

```yaml
api:
  listen_addr: 127.0.0.1:8080

proxy:
  http_listen_addr: 127.0.0.1:1081
  socks_listen_addr: 127.0.0.1:1080
```

如果现有配置已经类似，就不重复改大结构。

### 10.5 验收标准

- `npm run build` 成功。
- Go 后端能托管前端页面。
- 访问 `/api/v1/system/health` 仍然返回 API。
- 访问 `/` 返回前端页面。
- 刷新前端任意路由不 404。
- `go test ./...` 通过。

## 11. 阶段六：管理端安全收口

### 11.1 目标

当前 README 已经提醒：

```text
API 当前建议只监听本地地址
```

下一步如果要让前端管理台更安全，需要补一个最小管理端鉴权。

### 11.2 建议方案

本阶段只做简单管理令牌，不做复杂用户体系。

配置：

```yaml
admin:
  token: "自己设置一个长一点的随机字符串"
```

请求方式：

```text
Authorization: Bearer <token>
```

前端保存位置：

```text
localStorage
```

### 11.3 不做复杂权限

本阶段不做：

- 管理员账号密码登录。
- 多角色权限。
- 用户邀请。
- 操作审计。
- 二次验证。

原因：

```text
当前是本地/内网管理后台，先用简单 token 把风险压住。
```

### 11.4 验收标准

- 未带 token 请求管理 API 会被拒绝。
- 配置为空时可以保持本地开发友好模式。
- 前端能填写并保存 token。
- API 请求自动带 token。
- 代理 HTTP/SOCKS5 凭证逻辑不受影响。

## 12. 推荐执行顺序

建议严格按下面顺序做：

```text
1. 系统诊断页增强
2. 节点页增强
3. 凭证页体验补强
4. 订阅详情与刷新日志增强
5. 前后端一体化托管
6. 管理端安全收口
```

原因：

- 先做诊断，后面排错效率更高。
- 再做节点页，因为节点是代理池最核心资产。
- 再补凭证页，让用户能看懂调度范围。
- 再补订阅页，让节点来源更清楚。
- 最后做一体化托管和安全收口，方便真正使用。

## 13. 每阶段提交规则

每完成一个可独立验收的小阶段，并且测试正常后，可以提交。

推荐提交：

```bash
git add .
git commit -m "feat: improve system diagnostics"
git commit -m "feat: enhance node management page"
git commit -m "feat: improve credential binding visibility"
git commit -m "feat: enhance subscription refresh details"
git commit -m "feat: serve embedded web dashboard"
git commit -m "feat: add admin api token"
```

实际提交信息按最终改动调整。

## 14. 测试策略

### 14.1 后端测试

每次后端改动后执行：

```bash
GOCACHE=/tmp/jnm-go-cache go test ./...
```

### 14.2 前端构建

每次前端改动后执行：

```bash
cd web && npm run build
```

### 14.3 手动验证

关键手动验证：

```bash
go run -tags "with_quic with_utls" ./cmd/jnmproxy
```

然后测试：

```bash
curl http://127.0.0.1:8080/api/v1/system/health
curl --proxy socks5h://用户名:密码@127.0.0.1:1080 https://httpbin.org/ip
```

## 15. 风险与处理

### 15.1 前端页面越做越乱

处理：

- 继续保持深色后台风格。
- 复用现有 UI 组件。
- 不在页面里写重复请求逻辑。
- 表格、抽屉、弹窗保持一致。

### 15.2 节点页数据过多

处理：

- 先做筛选。
- 后续节点特别多时再做分页或虚拟列表。
- 本阶段不提前过度优化。

### 15.3 管理端鉴权影响开发

处理：

- token 为空时允许本地开发。
- token 配置后才启用鉴权。
- 明确文档提醒不要公网裸奔。

### 15.4 一体化托管影响代理入口

处理：

- API/前端服务和代理服务保持端口职责清楚。
- 不让前端静态路由抢 `/api/v1`。
- 不改变 HTTP/SOCKS5 代理认证逻辑。

## 16. 最终验收标准

这一整份计划完成后，JnmProxy 应该达到：

- 用户能通过系统页快速判断运行能力。
- 用户能通过节点页定位节点失败原因。
- 用户能通过凭证页看懂每个账号的调度范围。
- 用户能通过订阅页看懂订阅刷新结果。
- 后端能直接托管前端页面。
- 管理 API 有基础安全保护。
- 代理入口功能不倒退。
- `go test ./...` 通过。
- `npm run build` 通过。

## 17. 最终结论

下一步最推荐先做：

```text
系统诊断页增强
```

原因：

- 用户之前已经遇到过节点看似可用但代理失败的问题。
- 诊断页能直接帮助判断 `with_quic`、`with_utls`、sing-box、健康检查和协议支持情况。
- 这一步完成后，后续调试节点页、订阅页、凭证页都会更轻松。

所以执行本计划时，第一阶段应从：

```text
web/src/pages/SystemPage.tsx
```

以及必要的：

```text
internal/api/server.go
```

开始。
