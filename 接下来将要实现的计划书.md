# JnmProxy 接下来将要实现的计划书

> 目标：在当前“节点管理、操作日志、全平台打包”已经完成的基础上，下一阶段把系统继续往“查得快、失败自动兜底、运行态看得懂”的方向推进。

## 1. 当前看到的问题

管理后台顶部现在还有一处占位：

```text
搜索节点、订阅、分组（后续接入）
```

这说明后台已经预留了全局搜索入口，但还没有真正实现。

另外，最近测试代理时也发现一个真实使用场景：

```text
如果某次请求选中的节点刚好不可用，本次请求会失败。
```

当前系统已经会记录失败，并在连续失败后临时避开这个节点，但还没有做到：

```text
同一次请求内自动换另一个节点继续尝试。
```

所以下一阶段最应该做的不是继续堆协议，而是把这几个使用体验补齐。

## 2. 下一阶段主线

本阶段主线建议定为：

```text
JnmProxy 搜索、自动兜底与运行态可观察阶段
```

大白话：

```text
用户找东西要快。
节点坏了要自动换。
系统为什么这样选、为什么失败，要能看懂。
```

## 3. 本阶段要实现的功能总览

### 3.1 全局搜索

把顶部占位搜索框做成真正可用。

支持搜索：

- 节点。
- 订阅。
- 分组。
- 凭证。
- 操作日志。

搜索结果应该能直接跳转：

- 节点结果：跳到节点页，并带搜索关键词或打开详情。
- 订阅结果：跳到订阅详情页。
- 分组结果：跳到分组页。
- 凭证结果：跳到凭证页。
- 操作日志结果：跳到操作日志页并带关键词。

### 3.2 请求失败自动换节点

当前逻辑：

```text
一次请求只选一个节点。
节点失败，本次请求失败。
连续失败后，节点会进入短期熔断。
```

目标逻辑：

```text
一次请求可以尝试多个不同节点。
第一个节点失败后，立刻换下一个节点。
最多尝试 N 个节点。
全部失败才返回失败。
```

例如：

```text
请求进来
选中 A 节点
A 连接失败
记录 A 失败
排除 A
选中 B 节点
B 成功
请求正常返回
记录 B 成功
```

### 3.3 运行态节点状态可视化

现在数据库里能看到健康状态，但内存里的运行态还不够直观。

需要让前端能看到：

- 节点当前是否处于内存熔断中。
- 当前连续失败次数。
- 熔断到什么时候恢复。
- 最近一次代理请求失败原因。
- 当前是否还在候选池里。

### 3.4 代理请求失败日志

已有的“操作日志”主要记录管理动作：

- 刷新订阅。
- 修改凭证。
- 删除节点。
- 执行批量操作。

但代理请求失败还没有专门的排错视图。

下一步建议新增轻量的“代理请求日志”或“运行日志”，记录：

- 入口协议：HTTP / SOCKS5。
- 使用的凭证。
- 目标地址。
- 尝试过哪些节点。
- 哪个节点成功。
- 哪些节点失败。
- 失败原因。
- 请求耗时。

为了防止数据库压力，默认可以只记录失败请求，成功请求仍然走现有流量统计。

### 3.5 配置化兜底参数

当前失败阈值和熔断时间是代码里的默认值：

```text
连续失败 3 次
熔断 1 分钟
```

下一步建议放进配置：

```yaml
runtime:
  max_attempts_per_request: 3
  failure_threshold: 3
  circuit_break_seconds: 60
  record_failed_requests: true
```

这样不同机器、不同节点质量可以自己调。

## 4. 本阶段不做的事情

为了避免偏离主线，本阶段不做：

- 不引入 Clash 内核。
- 不替换 sing-box。
- 不做复杂权重调度。
- 不做多用户权限系统。
- 不做公网云端同步。
- 不做移动端 App。
- 不做规则分流。
- 不做自动购买订阅。
- 不做数据库切换到 MySQL/PostgreSQL。

本阶段只围绕：

```text
搜索、失败自动兜底、运行态排错
```

## 5. 阶段一：全局搜索

### 5.1 后端接口

新增接口：

```text
GET /api/v1/search?q=关键词
```

返回结构建议：

```json
{
  "query": "hk",
  "items": [
    {
      "type": "node",
      "id": 1,
      "title": "香港 HK 01",
      "subtitle": "节点 / vmess / 1.2.3.4:443",
      "url": "/nodes?search=香港"
    }
  ]
}
```

### 5.2 搜索范围

第一版支持：

- `proxy_nodes.name`
- `proxy_nodes.server`
- `subscriptions.name`
- `subscriptions.url`
- `proxy_groups.name`
- `credentials.username`
- `operation_logs.message`
- `operation_logs.action`

### 5.3 前端交互

修改：

```text
web/src/components/layout/AppShell.tsx
```

实现：

- 顶部搜索框可点击。
- 输入关键词后显示下拉结果。
- 支持键盘上下选择。
- 支持回车跳转。
- 支持 `Ctrl + K` 或 `/` 快捷打开。
- 没结果时显示“没有匹配结果”。
- 保持当前深色后台风格。

### 5.4 验收标准

- 顶部不再显示“后续接入”。
- 输入节点名能搜到节点。
- 输入订阅名能搜到订阅。
- 输入分组名能搜到分组。
- 点击结果能跳到对应页面。
- `npm run build` 通过。
- `go test ./...` 通过。

## 6. 阶段二：请求失败自动换节点

### 6.1 后端核心思路

当前入口：

```text
internal/proxy/http.go
internal/proxy/socks5.go
```

当前选节点：

```text
Cache.Select(username)
```

下一步需要新增一个“尝试选择器”能力：

```text
第一次选节点
失败后排除该节点
继续选下一个节点
最多尝试 max_attempts_per_request 次
```

建议新增方法：

```go
SelectExcluding(username string, excludedNodeIDs map[int64]struct{}) (Selection, error)
```

或者：

```go
NewSelectionSession(username string)
session.Next()
session.ReportFailure()
session.ReportSuccess()
```

为了不大改结构，第一版建议用 `SelectExcluding`，更简单。

### 6.2 HTTP 代理行为

HTTP CONNECT：

- 第一次节点失败，还没向客户端写 `200 Connection Established` 前，可以换节点重试。
- 一旦已经写了 `200` 并开始转发，就不能再透明换节点。

普通 HTTP 请求：

- 如果连接上游失败，可以换节点。
- 如果请求已经写入上游但读取响应失败，第一版建议也算失败，但不要无限重试。

### 6.3 SOCKS5 代理行为

SOCKS5：

- 在返回成功响应给客户端之前，可以尝试多个节点。
- 如果最终成功，再给客户端写 SOCKS5 成功回复。
- 如果全部失败，再返回 SOCKS5 失败码。

### 6.4 失败记录

每次节点失败都要：

- 调用 `ReportNodeFailure`。
- 记录失败统计。
- 记录尝试详情。

成功后：

- 调用 `ReportNodeSuccess`。
- 记录成功统计。

### 6.5 验收标准

- 当第一个随机节点不可用时，同一次请求能自动尝试第二个节点。
- 最多尝试次数可配置。
- 不会重复尝试同一个节点。
- 全部失败才返回客户端失败。
- 成功节点会清理内存失败状态。
- `go test ./internal/proxy ./internal/cache` 通过。

## 7. 阶段三：运行态节点状态 API

### 7.1 后端接口

新增接口：

```text
GET /api/v1/runtime/nodes
```

返回每个运行态节点：

- `node_id`
- `failure_count`
- `circuit_open`
- `circuit_until`
- `in_candidate_pool`
- `last_failure`

也可以先做节点详情接口扩展：

```text
GET /api/v1/nodes/{id}/runtime
```

第一版建议先做：

```text
GET /api/v1/runtime/nodes
```

因为节点页可以一次性加载后按节点 ID 映射。

### 7.2 前端展示

修改：

```text
web/src/pages/NodesPage.tsx
```

展示：

- 熔断中标签。
- 连续失败次数。
- 恢复倒计时或恢复时间。
- 节点是否在候选池。

节点详情抽屉里增加：

```text
运行态诊断
```

### 7.3 验收标准

- 节点页能看到哪些节点正被临时避开。
- 节点详情能看到为什么不进入随机池。
- 请求失败后刷新页面能看到失败计数变化。
- 熔断结束后节点能重新进入候选池。

## 8. 阶段四：代理请求失败日志

### 8.1 数据表

新增轻量表：

```sql
proxy_request_logs
```

字段建议：

- `id`
- `entry_protocol`
- `credential_id`
- `username`
- `target_address`
- `status`
- `attempt_count`
- `selected_node_id`
- `selected_node_name`
- `error`
- `attempts_json`
- `duration_ms`
- `created_at`

第一版只记录失败请求。

### 8.2 后端接口

新增：

```text
GET /api/v1/proxy-request-logs
```

支持分页和搜索：

- `page`
- `page_size`
- `search`
- `status`
- `entry_protocol`

### 8.3 前端页面

新增页面：

```text
web/src/pages/ProxyRequestLogsPage.tsx
```

侧边栏名称：

```text
请求日志
```

展示：

- 时间。
- 入口协议。
- 凭证。
- 目标地址。
- 状态。
- 尝试次数。
- 最终节点。
- 失败原因。
- 尝试详情。

### 8.4 验收标准

- 代理请求失败后，页面能看到失败日志。
- 能看到尝试过哪些节点。
- 能看到每个节点失败原因。
- 日志分页正常。
- 不明显增加数据库压力。

## 9. 阶段五：配置项与文档收口

### 9.1 配置文件

修改：

```text
internal/config/config.go
config.example.yaml
```

新增：

```yaml
runtime:
  max_attempts_per_request: 3
  failure_threshold: 3
  circuit_break_seconds: 60
  record_failed_requests: true
```

### 9.2 README

补充：

- 自动换节点说明。
- 失败重试次数说明。
- 熔断规则说明。
- 请求日志说明。

### 9.3 成果书

更新：

```text
成果书.md
```

用大白话说明新能力。

## 10. 推荐执行顺序

严格按下面顺序做：

```text
1. 全局搜索
2. 请求失败自动换节点
3. 运行态节点状态 API
4. 代理请求失败日志
5. 配置项与文档收口
```

原因：

- 全局搜索是界面上已经暴露的占位，应该先补齐。
- 自动换节点直接影响代理成功率，优先级高。
- 运行态状态能帮助验证自动换节点是否正常。
- 请求日志最后补，可以把前面所有尝试细节沉淀下来。
- 最后统一配置和文档，避免边做边改乱。

## 11. 测试策略

### 11.1 后端测试

执行：

```bash
GOCACHE=/tmp/jnm-go-cache go test ./...
```

针对自动换节点，重点测试：

```bash
go test ./internal/proxy ./internal/cache
```

### 11.2 前端测试

执行：

```bash
cd web
npm run build
```

### 11.3 打包验证

执行：

```bash
./scripts/build-all-platforms.sh
```

### 11.4 手动验证

启动：

```bash
./jnmproxy -config config.yaml
```

验证全局搜索：

```text
顶部搜索输入节点名、订阅名、分组名。
```

验证自动换节点：

```text
准备一个坏节点和一个好节点。
把凭证绑定到同一个分组。
请求代理。
确认坏节点失败后能自动尝试好节点。
```

## 12. 风险与处理

### 12.1 自动重试导致请求变慢

处理：

- 默认最多尝试 3 个节点。
- 每个节点仍受拨号超时控制。
- 后续可以把超时时间单独配置。

### 12.2 非幂等 HTTP 请求重复发送风险

处理：

- CONNECT 在写成功响应前重试，风险较低。
- 普通 HTTP 请求第一版只在“还没成功连上上游”时重试。
- 对已经写入上游的请求，不做复杂重放。

### 12.3 日志太多压数据库

处理：

- 第一版只记录失败请求。
- 成功请求仍然只进入流量统计。
- 后续再加日志保留天数或清理任务。

### 12.4 全局搜索查太多表变慢

处理：

- 每类结果限制数量，比如每类最多 5 条。
- 搜索为空不请求。
- 前端输入做 debounce。
- 后端只查关键字段。

## 13. 本阶段最终验收标准

本阶段完成后，系统应该达到：

- 顶部全局搜索可用。
- 可以快速搜索节点、订阅、分组、凭证、日志。
- 代理请求遇到坏节点时，可以自动换节点重试。
- 节点短期熔断状态能在前端看到。
- 代理失败请求有日志可查。
- 关键兜底参数可配置。
- `go test ./...` 通过。
- `npm run build` 通过。
- `scripts/build-all-platforms.sh` 能正常打包。

## 14. 第一阶段建议先做什么

建议先从：

```text
全局搜索
```

开始。

理由：

- 界面上已经有明显占位。
- 风险低，不影响代理核心转发。
- 完成后管理体验立刻提升。
- 后续请求日志、节点运行态也能接入同一个搜索入口。

